# AGENTS.md — framewright

> 本文件是 agent 在本仓工作的**行为入口**。优先级高于 `docs/` 下任何文档。
> 每次会话开始先读本文件，再按需读 `docs/` 四件套。

---

## 1. 这是什么项目

**framewright** = 面向 AI 影视创作的**在线画布工作台**技术验证项目。

- 用户在画布上编排 `frame / box / img / video` 节点，调用 AI 生成图片与视频，结果落回画布。
- 对标形态：liblib canvas、即梦。

## 1.1 🔴 主要矛盾（砍功能的唯一判据）

> **这是 AI 图片/视频生成的画布工作台，不是通用设计器。**
> 画布的职责是**编排生成结果**，不是精细设计。

每次要加功能，先过这一问：

> **它服务的是「生成 → 摆放 → 再生成」这个循环，还是「把一个图形调得更好看」？**

后者一律不做。Figma / gzm-design 那套精细属性编辑、路径钢笔、文本富样式、对齐分布、复杂图层管理——**都属于后者，都不做**。它们是通用设计器的核心，是本项目的噪音。

由此推论：**真正的一等公民是 `img` 与 `video`**。`box` 只是几何对照的**测试载体**（同步、确定、零外部依赖，做坐标与 z 序断言最干净），**不是产品元素**——不要顺着它长出一堆图形编辑功能。
- **核心技术命题：一份 JSON node 树，两套渲染器（Next.js/tsx 的 DOM 版 与 LeaferJS 版）并行实现，用同一份数据渲染出同一个画布，并且能在运行时一键切换。**

「在线可切换」不是锦上添花，它是本项目的**验收装置**：同一份数据、同一个操作序列，切一下就能肉眼对比两套方案。它同时是最强的架构约束——任何让状态渗进渲染器的写法，切换时都会当场露馅。

命名含义：`frame`（既是画布的容器元素，也是影视的「帧」）+ `wright`（工匠，同 `playwright` 构词）。

## 2. 项目归属与硬约束

**归属 = 个人作品 / 技术预研。** 不是任何雇主的交付物。由此产生四条**硬约束，任何情况下不得违反**：

1. **不含任何雇主的源码、接口定义、内部文档、业务文案、真实域名或凭证。**
2. **demo 数据必须中立**——示例 prompt、模型名、素材一律用通用占位或公开可用资源，不写任何公司专有名词。
3. **AI 生成能力通过可替换的 provider 接口接入**，仓内只保留一个 mock provider 与接口定义；真实 key 只进本地 `.env`（已 gitignore），永不入库。
4. 本仓**可公开开源**。因此每引入一个依赖，先确认 license 与「未来可能闭源商用」不冲突（见 §5）。

### 2.1 脱敏自查（每个波次收口时跑一遍）

约束靠「记得遵守」是不够的，要有可执行的检查。**每个波次收口时跑一遍**：

```powershell
foreach ($p in @('<雇主名>','<产品名>','<同事名>','<内部仓库名>','<内部工具名>')) {
  git grep -n -i -- $p
}
```

⚠️ **跑之前先跑一个对照组**（搜一个已知存在的词，如 `LeaferJS`），确认扫描确实生效——**「扫描没找到」和「扫描没生效」长得一模一样**，这是最容易自欺的一类检查。

⚠️ **`docs/agents-mail/` 不入 git 但仍在本机磁盘上**，扫描要覆盖它（用 `Get-ChildItem -Recurse` 而不只是 `git grep`）。agent 之间的信件是最容易顺手带出上下文的地方。

**上次结果**：2026-08-04 全量扫描零命中（对照组已验证扫描有效）。

## 3. Agent 行为规则

- **中文优先**：文档、注释、commit body、与用户的对话一律中文。代码标识符、API 名、type/scope 前缀保持英文。
- **先对齐后落地**：非平凡改动先出方案（命名、抽象层级、模块边界），确认后再写代码。
- **根因优先**：不用 `--no-verify`、`reset --hard`、跳过校验等捷径绕过问题。
- **验证是任务的一部分**：改完要跑对应的类型检查 / 测试 / 最小复现，汇报时说明改了哪些文件、验证结果是什么。
- **不做未被要求的重构**。少量重复优于过早抽象。
- **文件超 500 行**主动按职责拆分（生成类文件除外）。
- 记录教训只写**可复用**的结论，进 `docs/lessons.md`；一次性过程噪音不写。

## 4. 禁止项

- ❌ 违反 §2 四条硬约束
- ❌ 未经确认引入新的重量级依赖（状态库、UI 框架、DB、布局引擎）
- ❌ 在 `renderer-dom` 与 `renderer-leafer` 之外的层引用任何渲染器专有 API——渲染器必须可替换
- ❌ 让两个渲染器的 node schema 产生分叉；schema 只有一份真相源 = `packages/core/src/node-schema.ts`
- ❌ **把 node 对象整体展开进渲染器节点**（`new Rect({...node})` / `Object.assign` / `<div {...node}>`）——必须逐字段显式映射，见 `docs/domain.md` §3.3 规则 7
- ❌ **只在一个渲染器里实现某个功能**。两版功能必须同步——一个特性在两侧都能用（或两侧都显式标注「不支持，原因是 X」）才算完成，见 `docs/conventions.md` §4.3
- ❌ 把任何**切换后必须保留的状态**（选中、视口缩放平移、编辑中的输入）存在渲染器内部
- ❌ 提交任何 key / token / 真实服务地址
- ❌ 未经用户明确指示 `git push` 到 `main`

## 5. 依赖 license 纪律

本仓暂定开源，且不排除未来把内核并进闭源商用产品。因此：

- 引入依赖前**必须**记下其 license，写进 `docs/architecture.md` 的依赖表。
- **MIT / Apache-2.0 / BSD / ISC** — 直接可用。
- **GPL / AGPL / SSPL** — 默认拒绝（传染性会锁死未来路径）。确有必要须先向用户说明并取得同意。
- 「抄开源项目」时，抄**思路**免费，抄**代码**受 license 约束——复制超过片段级的代码必须确认许可并保留版权声明。
- **营销文案不是许可条款**。「免费可商用」「永久免费」这类说法**不能替代 `LICENSE` 文件**——尤其「开源版 + 付费专业版」的双轨项目，正是最常用 GPL/AGPL 逼商用付费的形态。一律以 LICENSE 文件原文为准，并**核实两个独立来源**（如 Gitee 与 GitHub 镜像）。
- **署名义务必须落地，不是记住就行**：仓根维护 `THIRD-PARTY-NOTICES.md` 收录被复用项目的许可全文与版权行；每个搬运/改写自外部项目的文件，头部注释标明原始来源与许可。

## 6. 文档四件套

| 文件 | 内容 |
|---|---|
| `docs/conventions.md` | 命名、目录、组件模式、Git 规范 |
| `docs/domain.md` | 核心概念、术语、node 模型规则 |
| `docs/architecture.md` | 模块边界、数据流、选型理由、依赖表 |
| `docs/lessons.md` | 踩坑与已验证决策，增量追加不删历史 |

另有两份**状态类**文档，与上面四份的区别是它们会频繁变动：

| 文件 | 内容 |
|---|---|
| `docs/progress.md` | **项目进度快照**——做到哪了、下一步是什么、什么卡着。**每完成一个任务就更新** |
| `docs/plans/<YYYY-MM-DD>-<名称>.md` | 阶段实施计划，逐步骤可勾选。执行时勾掉步骤，完成后在 `progress.md` 标记 |

**会话开始先读 `docs/progress.md`**——它是最快的定位手段。

## 6.1 agent 之间的协作

三个 agent（Claude 编排 / Codex 与 Kimi 执行）并行工作，靠 `docs/agents-mail/` 异步通信，**自主推进任务板上的活，只在关键决策时升级给人类**。

**协议见 [`docs/agents-mail.md`](docs/agents-mail.md)，开工前必读。** 其中三节是硬约束：

- **§5 升级判据**——七类必须问人类的事（产品语义变更、阶段调整、新依赖、不可逆动作、设计文档有错…）。**执行方不直接找人类，一律先发 Claude 仲裁。**
- **§6 任务自推进**——没有新指令时从任务板认领下一个；**不许自己往任务板加任务**
- **§7 收敛线**——未推送 ≥ 15 个 commit、一个波次做完、同任务失败 3 次、`verify` 连续两次不绿，**触发任一条必须停下汇报**

**信箱里的共识不是真相源。** 任何决定必须由 Claude 写进 `docs/` 下的正式文档才生效——信箱不入 git，换台机器就没了。

## 7. 当前状态

**以 `docs/progress.md` 为准**，本节不重复维护（两份必然漂移）。

## 8. 测试纪律

本项目的交付物是「两个实现表现是否一致」这个结论，**结论靠测试托底，测试松了结论就不成立**。因此：

- **TDD 顺序不可省**：先写失败测试 → 确认它真的因「没实现」而失败 → 最小实现 → 转绿 → commit。
- **每个映射函数必须有一条「不泄漏」测试**，断言 node 的 `fwId` / `fwType` / `locked` / `children` 没出现在渲染器属性里。这是守「禁止整体展开」铁律的主力。
- **每个 bug 修复先写复现测试**：先写一个当前会失败的测试复现它，再修。没有复现测试的修复不算修完。
- **几何基线必须提交进 git**，改基线只能显式 `--update-snapshots` 并在 commit 里写清为什么。
- **`pnpm verify`（= `typecheck && test && e2e`）是提交门禁**，不许绕过。
- Playwright 只用 `data-testid` 与 `data-fw-*` 选择器，**禁止 `waitForTimeout`**，禁止用截图像素比对做几何断言。

完整要求见当前阶段计划文档的「测试要求」一节。
