# 任务板 —— P0 之后的全部编码任务

> 设计已基本定案，转入快速执行。本文件是**任务的单一真相源**：编号、依赖、归属、并行波次。
> 每个任务的详细做法查它引用的设计文档，**本板不重复设计内容**。
>
> 建立日期：2026-08-03。执行方：Codex 与 Kimi。编排与规格：Claude。

---

## 0. 阶段重排（2026-08-03，Claude 的判断）

原顺序 `P1 持久化 → P2 交互` 是按工程直觉排的（骨架先于交互），**不是按「对选型结论的贡献」排的**。按贡献重排：

| 对选型结论的贡献 | 事项 | 原阶段 | 新阶段 |
|---|---|---|---|
| 🔴 可能**直接决定**结论 | 视频节点内嵌播放器（开环 #9） | P1 | **P1a** |
| 🔴 「使用体验」维度**完全依赖**它 | 交互（拖拽/缩放帧率、跟手感） | P2 | **P1b** |
| 🟡 「多节点渲染性能」的真实负载 | `ai-image`/`ai-video` 真实渲染 | P1 | **P1a** |
| ⚪ **零贡献** | 持久化 / 后端 / 撤销 | P1 | **P1c** |

**持久化对「DOM 还是 LeaferJS」贡献为零**——后端与渲染器选型完全无关。它服务的是 MVP 判据里的「作品可展示」，不是「选型结论」，却排在最前面。

**重排后的收益**：选型结论在 **P1a + P1b 结束时就出来了**。若 Leafer 在视频播放器或交互上代价高到不可接受，可当场止损，不必把持久化那一整块也做两遍。

| 阶段 | 内容 | 产出 |
|---|---|---|
| **P1a 渲染能力** | core 纯函数、schema 扩展、生成单元四态、连线、**视频播放器实测** | 多节点渲染性能与媒体能力的对照数据 |
| **P1b 交互** | 无限画布全套，契约已定案 | 使用体验维度的对照数据 → **选型结论可出** |
| **P1c 持久化** | 后端、`documentId`、撤销日志、provider mock | 端到端跑通、作品可展示 |

---

## 1. 任务板

**图例**：⬜ 未开始 ｜ 🚧 进行中 ｜ ✅ 完成
**并行**：同一波次内的任务彼此无依赖，可同时开工。

### 在飞（已派发）

| # | 任务 | 执行 | 依赖 | 状态 |
|---|---|---|---|---|
| T13 | 可见性级联进 core + `getVisibleNodeIds()` | Codex | — | 🚧 |
| T14 | `renderer-leafer` 场景图改嵌套 | Codex | T13 | 🚧 |

### 波次 1 —— core 纯函数（**全部彼此独立，最适合并行**）

全部是纯 TS、零渲染依赖、可单测覆盖。签名已在 `docs/renderer-contract.md` §5 给定，**照抄，不要自行改签名**。

| # | 任务 | 执行 | 依赖 | 状态 |
|---|---|---|---|---|
| A1 | `core/src/viewport.ts` —— `screenToCanvas` / `canvasToScreen` / `panBy` / `zoomAtPoint` / `clampScale` / `normalizeWheelSteps` / `getContentBounds` | Codex | — | ✅ `aec07a7` |
| A2 | `core/src/hit-test.ts` —— `rectFromPoints` / `intersects` / `collectNodesInRect` / `hitTestPoint` | Codex | — | ✅ `16b85c8` |
| A3 | `core/src/selection.ts` —— `applySelection` | Kimi | — | ✅ `4c2344f` |
| A4 | `core/src/transform.ts` —— `computeMoves` / `resizeProportional` | Kimi | — | ✅ `2d3bea5` |
| B1 | schema 扩展：`ai-image` / `ai-video` + `sourceFwIds` + 工厂 + 守卫 + demo 文档扩充 | Kimi | — | ✅ `741b19d` |

**A1 要点**：`zoomAtPoint` 的硬性语义是「光标下的画布点缩放前后不动」，这是最容易实现错的地方（错了表现为画布往光标反方向滑走）。`normalizeWheelSteps` 见契约 §5 的说明——鼠标大 delta 与触控板连续 delta 的归一化常数必须统一。

**A2 要点**：框选是**相交**不是完全包含；排除 `locked` 与 root；`rotation` 首版按 AABB（契约裁决 9）。

**A4 要点**：`computeMoves` 必须处理「父子同选时只保留最上层」（防后代被移动两次），且输出是**父相对坐标 + `parentFwId`**（契约 §2）。

**B1 要点**：见 `docs/domain.md` §3.2.1 与 §3.2.2。`sourceFwIds` 只在生成单元上。demo 文档要加**至少一组溯源关系**（一个 `ai-image` → 两个 `ai-video`），供后续连线渲染与测试使用。

### 波次 2 —— 生成单元与连线渲染（两侧并行）

| # | 任务 | 执行 | 依赖 | 状态 |
|---|---|---|---|---|
| C1-dom | `renderer-dom` 的 `ai-image`/`ai-video` 四态渲染 | Codex | B1 + M1 | ✅ `6e4740b` |
| C1-leafer | `renderer-leafer` 的 `ai-image`/`ai-video` 四态渲染 | Kimi | B1 + M1 | ✅ `c3ed170` + `2af5443` |
| C2-dom | `renderer-dom` 的溯源连线渲染 | Codex | B1 + M2 | ✅ `a5917f2` |
| C2-leafer | `renderer-leafer` 的溯源连线渲染 | Kimi | B1 + M2 | ✅ `2285142` |
| C2-core | **收编锚点提取逻辑到 core**（右中点→左中点、悬空跳过）——两侧现各写一份，同 `k` 公式的同款分叉风险 | Kimi | C2 两侧 | ⬜ |
| **C3** | 🔴 **视频播放器能力实测**（两侧各做，产出对照数据） | 两侧各一 | C1 | ⬜ |

### 波次 2.5 —— `D0-min`（补波次 2 的依赖倒置，**波次 2 结束后立即做**）

| # | 任务 | 执行 | 依赖 | 状态 |
|---|---|---|---|---|
| D0-min-core | `RenderContext` 加 `callbacks`；`RendererCallbacks` 接口按契约 §2 落地；`RendererHost` 提供实现 | Codex | C1/C2 两侧完成 | ⬜ |
| D0-min-dom | `renderer-dom` 把内部按钮接上 `onNodeAction`，补 M1 §7 验收 4 的测试 | Codex | D0-min-core | ⬜ |
| D0-min-leafer | `renderer-leafer` 同上 | Kimi | D0-min-core | ⬜ |

**为什么会有这一档**：M1 §5 要求内部按钮触发 `onNodeAction`，但 `callbacks` 的接口扩展被排到了波次 3 的 D0——**规格依赖了尚未存在的接口，是我排任务时的依赖倒置**。Codex 在 C1-dom 开工核对时发现并按纪律停下上报（工作区保持干净、未写一行代码），裁定见 `docs/agents-mail/`。

**为什么叫 min**：只把 `onNodeAction` 这一条链路跑通，其余六个回调**只定接口不接实现**——那些要等 D1 的手势状态机。

**为什么不在波次 2 里就地修**：当时 Kimi 正在飞 C1-leafer。`callbacks` 同时牵动 `core` 与两侧渲染器，**在它跑到一半时改 `core` 会打断它的运行**——共享 checkout 下改公共依赖的典型风险。

**C3 是本轮最重要的任务**（开环 #9）。它不是「把视频显示出来」，是**实测两个方案在「画布内嵌可播放播放器」上的真实代价**：播放/暂停、进度条、时间显示、音量、多个视频同时播放。结论直接写进 `docs/architecture.md` §8.2。

### 🎯 里程碑 M-可看 —— 最小可用的无限画布（用户 2026-08-04 拍板优先做）

**目标：让用户能真的在画布上平移、缩放，并一键切换两个渲染器对比手感。**

这是从波次 3 里切出来的一刀，**刻意跳过 D2/D3/D4**（框选/拖拽/键盘）。理由：「使用体验」是选型三维度里唯一完全依赖交互的一维，而**平移缩放就足以产生手感差异**——早 3–4 轮拿到用户的真实反馈，比按顺序做完整波次 3 更值。

代价：中间会有一段「能平移缩放但不能选中拖拽」的半成品状态，接受。

| # | 任务 | 执行 | 依赖 | 状态 |
|---|---|---|---|---|
| D0-min-core | `RenderContext` 加 `callbacks`；`RendererCallbacks` 按契约 §2 落地；`RendererHost` 提供实现 | Codex | — | ✅ `4640f18` |
| D0-min-dom | `renderer-dom` 接 `onNodeAction`，补 M1 §7 验收 4 | Codex | D0-min-core | ✅ `e9a4356` |
| D0-min-leafer | `renderer-leafer` 同上 | Kimi | D0-min-core | ✅ `ea43ed7` |
| **D1-dom** | 手势状态机 + 中键/空格平移 + `Ctrl`+滚轮锚点缩放 | Codex | D0-min-core | ✅ `ed3cc1c` |
| **D1-leafer** | 同上。~~内建手势逐项 disabled~~ → 实测后收窄，见契约 §3.1 | Kimi | D0-min-core | 🚧 |
| D-host | `RendererHost` 接 `onViewportChange`、维护视口会话状态、显示缩放比例 | Codex | D1 两侧 | ✅ `8a1b1dc` |

**D1-leafer 是本里程碑的最高风险项**：Leafer 内建手势（`move.holdMiddleKey` / `move.holdSpaceKey` / `wheel.zoomMode` / `zoom.min/max` / UI 级 `draggable` / 内部 `onWheel`）**必须逐项显式禁用**，漏一个就是「画布自己动了但 host 不知道」。Kimi 在 T10 明说过「内建配置的默认值我没有逐项查全」——**先实测打印一遍默认值，别靠记忆**。

**验收**：拖动画布后立刻切换渲染器，断言 viewport 与切换前一致（契约 §3 的红线验收）。

> **2026-08-04 用户改令：「做完」** —— M-可看 不再作为独立交付点停下，直接连着做完整套交互（D2–D6）。
> 起因：用户看 demo 时发现「能平移缩放但不能拖节点」的半成品状态别扭，判断没有拖拽就没法真正对比手感。
> **原里程碑的切分仍然有效**（D0-min + D1 已完成，是真实的进度里程碑），只是不停下等反馈了。

### 波次 3 剩余 —— 交互补齐（M-可看 之后）

| # | 任务 | 执行 | 依赖 | 状态 |
|---|---|---|---|---|
| D0 | `RendererAdapter` 接口加 `callbacks`，`RenderContext` 扩展 | Codex | A1–A4 | ⬜ |
| D1-dom | 手势状态机 + 平移缩放（A/B） | Codex | D0 | ⬜ |
| D1-leafer | 手势状态机 + 平移缩放（A/B）**含内建手势逐项 disabled** | Kimi | D0 | ⬜ |
| D2-dom | 框选 + 点选（C/D） | Codex | D1-dom | ⬜ |
| D2-leafer | 框选 + 点选（C/D） | Kimi | D1-leafer | ⬜ |
| D3-dom | 拖拽移动 + 等比缩放（E/G） | Codex | D2-dom | ⬜ |
| D3-leafer | 拖拽移动 + 等比缩放（E/G） | Kimi | D2-leafer | ⬜ |
| D4-dom | 键盘操作 + 光标反馈（H/J） | Codex | D2-dom | ⬜ |
| D4-leafer | 键盘操作 + 光标反馈（H/J） | Kimi | D2-leafer | ⬜ |
| D5 | 选中 overlay：不随缩放变粗、四角控制点、多选包围框 | 两侧各一 | D3 | ⬜ |
| D6 | 应用层：`RendererHost` 接全部回调 + 会话状态 + 工具栏 | Codex | D1–D4 | ⬜ |

**D1-leafer 的红线**：契约 §3——`move.holdMiddleKey` / `holdSpaceKey` / `wheel.zoomMode` / `zoom.min/max` / UI 级 `draggable` / 内部 `onWheel` **必须逐项显式 disabled**。漏禁一个就是「画布自己动了但 host 不知道」。**验收必须包含**：拖动画布后立刻切换渲染器，断言 viewport 不变。

### 🔄 U 系列 —— 撤销（用户 2026-08-04 提出，拆两段）

设计早已在 `docs/domain.md` §4.5（持久化操作日志、以 `documentId` 为键、200 条裁剪）。**拆两段是排期调整，不是设计变更。**

**⭐ 关键性质：撤销不是渲染器功能，是 host + core 的功能。** `applyOp` 操作的是 node 树，与画在 DOM 还是 canvas 无关——**所以它两版天然同步，不需要等两侧渲染器齐平**，也不占 `renderer-*` 的文件领地。

| # | 任务 | 执行 | 依赖 | 状态 |
|---|---|---|---|---|
| **U1a** | `core/src/history.ts` —— `CanvasOp` 判别联合 + `applyOp` / `invertOp` 纯函数 + 操作栈 | Codex | — | ⬜ |
| **U1b** | host 在七个变更回调处记录 `CanvasOp`；`Ctrl+Z` / `Ctrl+Shift+Z` 接线 | Codex | U1a | ⬜ |
| U2 | 操作日志落后端、以 `documentId` 为键、200 条裁剪、跨会话撤销 | 后续 | U1b + F2 | ⬜ |

**U1 纯前端零后端依赖**，做完就能撤销；U2 只是把同一份日志换个存储介质。同「后端选 Next.js 但业务逻辑收进 `server-core` 保留拆 NestJS 退路」的手法——**先做可逆的那部分**。

**紧迫性**：D2/D3 已经在产出 `onNodesMove` / `onNodesResize` / `onNodesDelete`，而 D6 已经把它们写回 node 树了——**也就是说此刻每一次拖动、缩放、删除都已经生效且不可撤销**。

### 📚 R 系列 —— 外部证据（不碰代码，与编码任务完全并行）

| # | 任务 | 执行 | 状态 |
|---|---|---|---|
| **R1** | 竞品渲染策略调研 | Claude 子 agent | ✅ `docs/research/2026-08-04-canvas-vs-dom.md` |

**R1 产出了一条必须提前做的任务**（见下 R2）。三条最有分量的发现：

1. **ComfyUI 正在从 Canvas 迁往 DOM**（官方 2025-12），理由是 canvas *"restrict[s] what we can do in the UI"* —— **这是在 AI 生成节点画布这个完全相同的场景里做的反向迁移**
2. **tldraw 的混合切法与直觉相反**：图形在 **DOM**，canvas 只画临时 overlay（选择框、笔刷、吸附线）
3. **tldraw 官方把 Luma 与 Runway 列为客户** —— 直接支持「AI 生成画布用 DOM」的观察（但样本仅 2 个、单方陈述）

| # | 任务 | 执行 | 状态 |
|---|---|---|---|
| **R2** | 🔴 **Leafer 侧 canvas 内文本输入可行性验证**（提前，不留到 G5） | Kimi | ⬜ |

**R2 为什么必须提前**：R1 查到 **Excalidraw 六年至今仍未解决 canvas 内的文本输入**——只能用绝对定位的 HTML overlay hack，失去原生 input 聚焦、文本选择、浏览器翻译、无障碍树同步（[#11378](https://github.com/excalidraw/excalidraw/issues/11378)，2026-05）。

而我们的 **G5 生成参数面板有 prompt 输入框**、节点可能有可编辑标题。**如果 Leafer 侧只能靠 DOM overlay 补，那本身就是选型结论的一部分**——必须在投入 G 系列之前验证，不能等做到一半才发现。

**为什么值得单开一条**：用户实测发现「竞品基本都用 DOM」，而初步搜索显示 **React Flow 是 DOM（每个节点是 React 组件）、tldraw 是 DOM+canvas+svg 混合**。这直接关系到最终选型结论的解释力——**如果竞品用 DOM 是因为「有 React Flow 可以直接用」而不是「canvas 性能不够」，那是完全不同的因果**，会改变结论的写法。

### 波次 4 —— 选型结论（阶段闸门）

| # | 任务 | 执行 | 依赖 | 状态 |
|---|---|---|---|---|
| E1 | benchmark harness：N=100/500/2000 节点的渲染耗时、拖拽帧率、缩放帧率、bundle 体积 | Kimi | D6 | ⬜ |
| E2 | 两侧实现成本报告（第二轮，覆盖 P1a+P1b） | 两侧各一 | D6 | ⬜ |
| M4 | **选型结论初稿** | Claude | E1 + E2 | ⬜ |

**到 M4 为止，「DOM 还是 LeaferJS」这个问题应该已经能回答了。** 后面的持久化不影响这个结论。

### 波次 5 —— 持久化（细拆版）

**领地**：`prisma/` + `packages/server-core/**` + `apps/web/app/api/**` + `apps/web/app/[lang]/canvas/**`。与 `renderer-*` 零重叠。

| # | 任务 | 执行 | 依赖 | 状态 |
|---|---|---|---|---|
| F1 | Prisma 接入 + `schema.prisma`：`Document` / `HistoryEntry` 两表，均以 `documentId` 为键；本地用 SQLite（`better-sqlite3`，同 `mingtu` 已验证路径） | Codex | — | ⬜ |
| F2 | `packages/server-core` 包骨架 + Document 存取（`getDocument` / `saveDocument` / `createDocument`），**纯 TS 零 Next 依赖** | Codex | F1 | ⬜ |
| F3 | `server-core` 的历史存取：追加 `CanvasOp`、按 `seq` 读取、**200 条裁剪**、`historySeq` 前进后退 | Kimi | F2 | ⬜ |
| F4 | Route Handlers 薄适配：`GET/PUT /api/documents/[id]`、`GET/POST /api/documents/[id]/history` | Codex | F2 | ⬜ |
| F5 | `/canvas/[documentId]` 路由 + 加载态 + 404 处理；首页列出/新建画布 | Codex | F4 | ⬜ |
| F6 | 前端接持久化：加载、**防抖自动保存**、保存失败的可见反馈 | Codex | F5 | ⬜ |
| F7 | **U2**：撤销历史接后端，跨会话撤销 | Kimi | F3 + U1b | ⬜ |
| F8 | e2e：刷新后画布还在、跨会话撤销、并发保存不互相覆盖 | Kimi | F6 + F7 | ⬜ |

### 波次 6 —— 生成能力（原 P3，细拆）

**领地**：`packages/provider/**` + `apps/web` 的生成面板。

| # | 任务 | 执行 | 依赖 | 状态 |
|---|---|---|---|---|
| G1 | `packages/provider`：接口定义 + **mock 实现**（提交→轮询→返回占位素材，可配置延迟与失败率） | Kimi | — | ⬜ |
| G2 | `server-core` 的生成任务编排：提交、轮询、结果落库、失败重试 | Kimi | G1 + F2 | ⬜ |
| G3 | Route Handlers：`POST /api/generate`、`GET /api/generate/[taskId]` | Codex | G2 | ⬜ |
| G4 | host 接生成流程：`onNodeAction('generate')` → `pending` → `running` → `succeeded`，四态在画布上真实流转 | Codex | G3 | ⬜ |
| G5 | 生成参数面板（浮层挂节点下方）：prompt 输入、模型/尺寸/时长选择、积分位（占位） | Codex | G4 | ⬜ |
| G6 | **派生生成**：选中一个 `ai-image` 发起生成 → 新节点自动建立 `sourceFwIds` 指向它 → 连线自动出现 | Kimi | G4 | ⬜ |
| G7 | e2e：完整生成循环（点击生成 → 骨架屏 → 结果落回 → 连线出现 → 刷新还在） | Kimi | G6 | ⬜ |

### 波次 7 —— 补齐开环与选型结论

| # | 任务 | 执行 | 依赖 | 状态 |
|---|---|---|---|---|
| **C3-dom** | 🔴 视频节点内嵌可播放播放器（开环 #9） | Codex | G4 | ⬜ |
| **C3-leafer** | 同上，用 `IVideoDecoder`（已实测存在：`play`/`pause`/`currentTime`/`volume`） | Kimi | G4 | ⬜ |
| E1 | benchmark harness：**按真实负载**测——N 张真实尺寸图片 + M 路同时播放视频，测首屏耗时、拖拽帧率、缩放帧率、内存、bundle 体积 | Kimi | C3 两侧 | ⬜ |
| E2-dom / E2-leafer | 两侧实现成本报告第二轮（覆盖 P1a+P1b+波次 5/6） | 两侧各一 | E1 | ⬜ |
| M3 | 综合两轮成本报告进 `architecture.md` §8.2 | Claude | E2 | ⬜ |
| **M4** | **选型结论初稿** | Claude | E1 + E2 + R1 | ⬜ |

### 随时可插的独立小任务（谁空了谁领）

这些**无依赖、文件不重叠**，是防止任一条链空转的缓冲池：

| # | 任务 | 领地 | 状态 |
|---|---|---|---|
| X1 | 工具栏 UI：缩放控件（`+`/`-`/适应画布/100%）、当前比例、渲染器切换做成正式控件 | `apps/web` | ⬜ |
| X2 | `Shift+1` 适应内容、`Ctrl+0` 适应画布 接线（`getContentBounds` 已在 core） | `apps/web` | ⬜ |
| X3 | 空画布态：没有节点时的引导 UI | `apps/web` | ⬜ |
| X4 | 键盘快捷键帮助面板（照 `interaction-spec.md` §6 的表） | `apps/web` | ⬜ |
| X5 | `renderer-leafer` 单测桩收敛：把 `leafer-test-stub.ts` 的四个桩整理成有文档的公共测试工具 | `renderer-leafer` | ⬜ |
| X6 | 两侧 shape 注册表的 unsupported 占位统一视觉（当前 DOM 斜纹 / Leafer 纯色，刻意不同，现可对齐） | 两侧 | ⬜ |
| X7 | `THIRD-PARTY-NOTICES.md` 补齐：gzm-design 的 MIT 全文与版权行（当前只有指针） | 根目录 | ⬜ |
| X8 | 脱敏自查脚本化：把 `AGENTS.md` §2.1 那段写成 `scripts/check-sensitive.mjs`，含对照组自检 | `scripts/` | ⬜ |

### Claude 的任务（挡住别人的先做）

| # | 任务 | 挡住 | 状态 |
|---|---|---|---|
| M1 | **生成单元视觉规格**（四态布局、尺寸、间距、骨架屏占位规则） | C1 两侧 | ⬜ |
| M2 | **连线视觉规格**（曲线形态、连接点位置、离群提示） | C2 两侧 | ⬜ |
| M3 | T9c：综合两份 P0 成本报告进 `architecture.md` §8.2 | — | ⬜ |
| M4 | 选型结论初稿 | — | ⬜ |

---

## 2. 通用纪律（每个任务都适用）

摘自 `AGENTS.md` 与 `docs/plans/2026-08-03-P0-编排与分工.md`，**不再逐份提示词重复**：

1. **先写失败测试 → 跑一遍确认它真的失败 → 再实现**，确认失败那步不许跳过
2. **只做分配给你的任务号**，不许顺手做下一个，不许「看到问题顺手改」别的文件
3. **不许改设计文档**。发现设计有错、有矛盾、写不出来 → **停下上报**
4. **不许 `git push`**，只 commit；**不许 `--no-verify`**；**不许 `--update-snapshots` 抹平基线变化**
5. **每个任务结束跑 `pnpm verify`**，全绿才算完成
6. **禁止把 node 对象整体展开进渲染器**（`{...node}`），必须逐字段显式映射
7. **任何切换后必须保留的状态不许存在渲染器内部**
8. **上层不许出现渲染器品牌分支**（`if (adapter.id === 'leafer')`）
9. **两版同步**：一个功能两侧都能用才算完成；一侧确实做不到，注册显式的 unsupported 实现 + 在成本报告写清技术原因
10. 回报用统一格式，**「接口摩擦」栏不许留空敷衍**
11. **🔴 不许改本任务板和 `docs/progress.md` 的状态**——它俩由 Claude 独占维护。你只管在回报里写清任务号与 commit sha，状态我来标。

### 为什么第 11 条要单列

两个执行方在同一个 checkout 上并行，**真实碰撞面只有这两个文件的状态列**——代码层面 `renderer-dom` 与 `renderer-leafer` 按包预先切开，天然不重叠。

考虑过给每个执行方开独立 git worktree（业界主流做法，Vibe Kanban 等都这么干），**但对本项目是净负担**：

1. **信箱在 `.gitignore` 里，而 ignored 文件在 worktree 之间不共享**——每个 worktree 各有一份 `docs/agents-mail/`，两边互相看不见对方的信，协作通道直接断掉
2. 两个渲染器都依赖 `@framewright/core`，分 worktree 后一边改了 core 另一边看不到（波次 3 的 D0 正是要改 core）
3. 每轮合并的负担全压到编排方

业界用 worktree 是因为他们的 agent 做的是**互相重叠的 feature**；我们是**按包预先切开的**，用不上那个隔离，却要吃它全部的代价。

**把状态列收归单一写者，就消掉了那两个文件的碰撞**，成本为零。

## 3. 冲突规避（共享 checkout，两个 agent 同时在飞）

**这条已经出过事**：T8 的代码被 `git add -A` 卷进了别人的 docs commit。

- **`git add` 前必须先 `git diff --stat`**，行数不对就说明有人插了活，只 add 自己的文件
- **不许用 `git add -A`**，按路径 add
- **不许 `reset` / `rebase` 别人的 commit**；发现自己的 commit 上压了别人的提交就放弃改写历史
- 波次内的任务已按**文件不重叠**划分：`renderer-dom` 与 `renderer-leafer` 天然隔离；core 的四个文件互不相干
