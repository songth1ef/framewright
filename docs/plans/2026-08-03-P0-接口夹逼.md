# P0「接口夹逼」实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 用两个渲染器实现从两边夹出正确的 `RendererAdapter` 接口形状，产出一个能在浏览器里一键切换 DOM / LeaferJS 两版画布的最小可运行系统。

**Architecture:** pnpm workspace monorepo。`@framewright/core` 是零渲染依赖的纯 TS 包，持有 node schema、树操作与 `RendererAdapter` 接口；`renderer-dom`（React/tsx）与 `renderer-leafer`（LeaferJS）实现同一接口、互不感知；`apps/web`（Next.js）里的 `RendererHost` 负责 `destroy() → mount()` 完成运行时切换。

**Tech Stack:** pnpm 11.10 / TypeScript 5.9.3 / Next.js 15.5.22 + React 19.2.8 / leafer-ui 2.2.9 / Vitest 4.1.10 (+ jsdom 27) / Playwright 1.62.1

## 版本选型（2026-08-03 实测 npm dist-tags 后定，非取 latest）

| 包 | 选定 | 当时 latest | 为什么这么选 |
|---|---|---|---|
| Node | `>=22`（本机 v22.23.1） | — | 22 是 LTS |
| pnpm | `11.10.0`（pin 在 `packageManager`） | — | 本机版本，锁死避免多机差异 |
| TypeScript | **5.9.3** | 7.0.2 | 🔴 **不用 7**。TS 7 是 Go 重写的原生编译器，刚 GA；ts-eslint、Next 的 type plugin、Vitest 的类型链路是否跟上未经验证。TS 在本项目是**基础设施不是研究对象**，取无聊的那个。TS 7 升级作为独立事项另评 |
| Next.js | **15.5.22** | 16.2.12 | 🔴 **不用 16**。理由不是「16 有问题」（我没有证据这么说），而是**有正面证据支持 15**：参考实现 `mingtu` 跑的就是 Next 15 + LeaferJS + React，这条集成路径是被实证过的。本项目在 Next 上的接触面只有一个页面 + 一次 `dynamic` 导入，**将来升 16 是半小时的孤立任务**；而 P0 期间撞上打包器边界问题会污染对照实验 |
| React / react-dom | **19.2.8** | 19.2.8 | ⚠️ **陷阱**：`npm view react@19 version` 返回的末项是 `19.0.8`，那是 **backport 分支的后发补丁，不是更新版本**。以 `dist-tags.latest` 为准 = 19.2.8。按发布时间排序会选错 |
| leafer-ui | **2.2.9** | 2.2.9 | 只有 `latest` 一个 dist-tag，发布模型简单。参考实现用的是 2.0.3，同主版本 |
| Vitest | **4.1.10** | 4.1.10 | 取当前稳定主版本。v5 已进 beta，v3 落后两个主版本会较快失去支持；`test.projects` 配置 API 在 3.2+ 与 4.x 都有。测试基建出问题是**显性**的，不会悄悄污染结论 |
| jsdom | **27.4.0** | 27.4.0 | 无争议 |
| @playwright/test | **1.62.1** | 1.62.1 | 无争议 |

### 🔴 依赖版本一律写精确值，且提交 lockfile

`leafer-ui` / `react` / `react-dom` / `next` **不写 `^`，写精确版本**。

理由是本项目特有的：**P0 之后要产出可复跑的性能对照数字**（见 `docs/architecture.md` §8.1）。依赖版本一漂移，不同时间跑出的数字就不可比，对照表会悄悄失真——而那张表是本项目的核心交付物。基准要能复现，依赖就必须能复现。

工具链依赖（TypeScript / Vitest / Playwright / jsdom / @types/*）不参与渲染，可以用 `^`。

## 测试要求（本项目的测试比一般项目更吃重）

原因很直接：**本项目的交付物是「两个实现表现是否一致」这个结论**。结论靠测试托底，测试松了结论就不成立。所以下面这些不是「最好有」，是**任务完成判据的一部分**。

### 三层分工与各自的边界

| 层 | 工具 | 覆盖什么 | **不**覆盖什么 |
|---|---|---|---|
| **单元测试** | Vitest（node / jsdom） | `core` 的纯逻辑（schema 默认值、类型守卫、树遍历、坐标累加、注册表校验）；两侧的**纯映射函数**（`toNodeStyle` / `toLeaferProps`） | 真实渲染、canvas、浏览器行为 |
| **回归测试** | Vitest + Playwright 基线快照 | 几何基线：任何改动导致节点位置尺寸变化，必须显式更新基线才能过 | 首次开发的新功能（那是上面一层的事） |
| **自动化测试** | Playwright（真实 Chromium） | Leafer 真实渲染、两侧几何对照、切换后状态保留与清理 | 纯函数逻辑（放单元测试更便宜） |

### 单元测试的具体要求

1. **TDD 顺序不可省**：先写失败测试 → 跑一遍确认它真的失败（且失败原因是「没实现」而不是「拼写错」）→ 写最小实现 → 跑通 → commit。本计划每个任务的步骤已按这个顺序排好。
2. **一个测试只断言一件事**。`it('补齐默认值并且守卫正确')` 这种要拆开——挂了才知道挂在哪。
3. **测试名写「行为」不写「函数名」**。`it('逐层累加父节点偏移')` 优于 `it('getAbsolutePosition works')`。
4. **必须覆盖边界与否定用例**，不能只测 happy path。本计划里已含的样板：找不到节点返回 `null`、注册表缺项要抛错且错误信息里点名缺哪几个、`visible:false` 渲染成 `display:none` 而不是从树里摘掉。
5. **🔴 每个映射函数必须有一条「不泄漏」测试**——断言 node 的 `fwId` / `fwType` / `locked` / `children` 没有出现在渲染器属性里。这条直接守着「禁止整体展开」铁律，是本项目最重要的单测。
6. **不测实现细节，测契约**。断言 `getRenderedBounds()` 的返回值，不断言内部用了什么数据结构。
7. `core` 的公开导出**每一个都要有测试**。`core` 是两个渲染器的共同地基，它错了两边一起错，对照实验测不出来。

### 回归测试的具体要求

回归防线有三道，缺一道就会出现「改 A 悄悄弄坏 B」：

1. **几何基线快照**（最重要）。把 demo 文档在两个渲染器下的 `getRenderedBounds()` 结果存成基线文件；每次跑 e2e 都与基线比对，**不一致就失败**。要改基线必须显式更新并在 commit 里说明为什么——这样「几何变了」永远是一个被人看见的决定，而不是一次静默漂移。
2. **每个 bug 修复必须先写复现测试**。顺序是：先写一个**当前会失败**的测试复现该 bug → 再修 → 测试转绿。没有复现测试的修复不算修完，因为没人能保证它不会回来。
3. **`pnpm verify` 是提交门禁**：`typecheck && test && e2e` 三条串起来，每次 commit 前跑一遍。**不允许用 `--no-verify` 之类的方式绕过**（见 `AGENT.md` §3 根因优先）。

### Playwright 自动化的具体要求

1. **选择器只用 `data-testid` 与语义属性**（`data-fw-id` / `data-fw-type`），**不用 CSS class、不用文本内容、不用 DOM 层级路径**——那三种一改样式就碎。
2. **用 Playwright 的自动等待，禁止 `waitForTimeout` 睡固定时间**。用 `expect(locator).toHaveText(...)` / `toHaveCount(...)` 这类会重试的断言。固定睡眠在慢机器上假失败、在快机器上白等。
3. **每个用例自带前置**，从 `page.goto('/')` 开始，不依赖别的用例留下的状态。用例之间必须能任意顺序、并行跑。
4. **断言要能指出问题在哪**。对照测试里逐节点断言并带上 `fwId` 作为消息（本计划 `parity.spec.ts` 已这么写），比整个对象一把比对更好定位。
5. **不用截图像素对比做几何断言**。抗锯齿与字体渲染在两个渲染器间天然不同，像素比对会给出一堆假失败。几何用 `getRenderedBounds()` 的数字断言——这也正是它作为正式接口存在的理由。

### 覆盖率

`pnpm test:coverage` 可跑，但**不设硬性百分比门槛**。理由：覆盖率数字容易被无断言的测试刷上去，反而掩盖问题。用上面第 7 条（`core` 每个公开导出都有测试）这种可检查的规则代替百分比。

## Global Constraints

以下为全局约束，**每个任务的要求都隐含包含本节**。来源为 `AGENT.md` 与 `docs/*.md`，值逐字照抄。

- **P0 只做静态渲染，无任何交互**（无拖拽、无框选、无缩放平移操作）。视口与选中态作为**数据**存在并参与渲染，但没有改变它们的 UI。
- **`core` 零渲染依赖**：不 import React，不 import leafer。
- **两个 `renderer-*` 互不依赖**，删掉任一个 `core` 仍能编译通过。
- **上层不允许出现 `if (adapter.id === 'leafer')` 之类的分支。**
- **🔴 禁止把 node 对象整体展开进渲染器节点**——不允许 `new Rect({...node})` / `Object.assign(leaferNode, node)` / `<div {...node}>`，必须逐字段显式映射。
- **任何切换后必须保留的状态都不能存在渲染器内部**：node 树、选中集、视口一律在 `core` / 应用层，经 `RenderContext` 单向流入。
- **两版同步**：`core` 导出 `SHAPE_TYPES` 全集，两个渲染器的 shape 注册表必须覆盖全集，缺一个在装载时报错。
- **schema 单一真相源** = `packages/core/src/node-schema.ts`。
- **命名**：framewright 语义字段加 `fw` 前缀（`fwId` / `fwType`）；几何呈现字段不加（`x` `width` …）。类型不加 `I` 前缀。文件 kebab-case。
- **TypeScript**：`strict: true` 且 `noUncheckedIndexedAccess: true`。禁止 `any`。
- **TypeScript 版本钉 `~5.9`**，不用刚发布的 7.0.x —— Next 16 + TS 7 的组合本计划未验证过，`mingtu` 已验证 5.9 可用。TS 7 升级作为独立事项另行评估，不在 P0 内。
- **Commit message**：英文 type/scope + 中文正文。
- **不 push**：P0 全程只 commit，push 由用户明确指示。

---

## File Structure

```
framewright/
├── package.json                       root：workspace 脚本入口
├── pnpm-workspace.yaml
├── tsconfig.base.json                 共享编译选项
├── vitest.config.ts                   两个 project：node 环境 / jsdom 环境
├── playwright.config.ts
├── THIRD-PARTY-NOTICES.md             第三方许可与版权声明
├── packages/
│   ├── core/src/
│   │   ├── node-schema.ts             判别联合 + SHAPE_TYPES + 守卫 + 工厂     【真相源】
│   │   ├── node-tree.ts               遍历 / 查找 / 绝对坐标累加
│   │   ├── renderer-adapter.ts        Viewport / RenderContext / RendererAdapter / 注册表校验
│   │   ├── demo-document.ts           P0 用的固定 demo 树（两侧与测试共用同一份输入）
│   │   └── index.ts                   包出口
│   ├── renderer-dom/src/
│   │   ├── node-style.ts              node → CSS 样式的逐字段映射（纯函数，可单测）
│   │   ├── shapes/registry.tsx        fwType → React 组件
│   │   └── index.tsx                  createDomRenderer(): RendererAdapter
│   └── renderer-leafer/src/
│       ├── node-props.ts              node → Leafer 属性的逐字段映射（纯函数，可单测）
│       ├── shapes/registry.ts         fwType → Leafer 节点工厂
│       └── index.ts                   createLeaferRenderer(): RendererAdapter
├── apps/web/
│   ├── next.config.ts
│   ├── app/layout.tsx
│   ├── app/page.tsx
│   └── components/renderer-host.tsx   持 container，装/卸 adapter + 切换开关
└── e2e/
    ├── leafer-render.spec.ts          Leafer 真实渲染（jsdom 跑不了 canvas）
    ├── parity.spec.ts                 两侧几何对照
    └── switch.spec.ts                 切换后状态不丢 + 旧渲染器清干净
```

**为什么 `getRenderedBounds()` 是正式接口而非测试后门**：对照实验是本项目的核心交付物，两个渲染器必须能自证「我把每个节点画在了哪」，否则 parity 无从验证（Leafer 画在 canvas 上，没有 DOM 元素可量）。它同时是一条有价值的工程约束。

**为什么 Leafer 的渲染测试走 Playwright**：LeaferJS 依赖真实 canvas 上下文，jsdom 无法提供。`core` 与 `renderer-dom` 走 Vitest，`renderer-leafer` 的真实渲染与全部对照/切换测试走 Playwright。

---

## Task 1: monorepo 脚手架与测试基建

**Files:**
- Create: `package.json` `pnpm-workspace.yaml` `tsconfig.base.json` `vitest.config.ts` `.gitignore` `THIRD-PARTY-NOTICES.md`
- Create: `packages/core/package.json` `packages/core/tsconfig.json` `packages/core/src/index.ts`
- Test: `packages/core/src/smoke.test.ts`

**Interfaces:**
- Consumes: 无
- Produces: workspace 布局；`pnpm test` / `pnpm typecheck` 两个根脚本；`@framewright/core` 包名可被其它包引用

- [ ] **Step 1: 创建 workspace 与根配置**

`pnpm-workspace.yaml`:
```yaml
packages:
  - 'packages/*'
  - 'apps/*'
```

`package.json`:
```json
{
  "name": "framewright",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22.0.0" },
  "packageManager": "pnpm@11.10.0",
  "scripts": {
    "typecheck": "tsc --build --force",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "e2e": "playwright test",
    "verify": "pnpm typecheck && pnpm test && pnpm e2e",
    "dev": "pnpm --filter @framewright/web dev"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^4.1.10",
    "@vitest/coverage-v8": "^4.1.10",
    "jsdom": "^27.4.0",
    "@types/node": "^22.20.1"
  }
}
```

`tsconfig.base.json`:
```json
{
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "moduleResolution": "bundler",
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "noUnusedLocals": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "declaration": true,
    "composite": true,
    "jsx": "react-jsx"
  }
}
```

`vitest.config.ts`:
```ts
import { defineConfig } from 'vitest/config'

export default defineConfig({
  test: {
    projects: [
      {
        test: {
          name: 'core',
          environment: 'node',
          include: ['packages/core/src/**/*.test.ts'],
        },
      },
      {
        test: {
          name: 'renderer-dom',
          environment: 'jsdom',
          include: ['packages/renderer-dom/src/**/*.test.{ts,tsx}'],
        },
      },
    ],
  },
})
```

`.gitignore`:
```
node_modules/
dist/
.next/
*.tsbuildinfo
test-results/
playwright-report/
.env
.env.local
```

- [ ] **Step 2: 创建 core 包骨架**

`packages/core/package.json`:
```json
{
  "name": "@framewright/core",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": { ".": "./src/index.ts" }
}
```

`packages/core/tsconfig.json`:
```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": { "outDir": "dist", "rootDir": "src" },
  "include": ["src"]
}
```

`packages/core/src/index.ts`:
```ts
export const CORE_PACKAGE_NAME = '@framewright/core'
```

- [ ] **Step 3: 写冒烟测试**

`packages/core/src/smoke.test.ts`:
```ts
import { describe, expect, it } from 'vitest'
import { CORE_PACKAGE_NAME } from './index'

describe('core 包骨架', () => {
  it('导出包名常量', () => {
    expect(CORE_PACKAGE_NAME).toBe('@framewright/core')
  })
})
```

- [ ] **Step 4: 安装依赖并运行测试**

```bash
pnpm install
pnpm test
```
Expected: 1 passed（`core` project 1 个用例；`renderer-dom` project 因无文件被跳过）

- [ ] **Step 5: 创建 THIRD-PARTY-NOTICES.md**

```markdown
# 第三方许可声明

本项目使用了以下第三方开源软件。

## LeaferJS

- 项目：https://github.com/leaferjs/LeaferJS
- 许可：MIT
- 用途：`@framewright/renderer-leafer` 的渲染底座（运行时依赖）

## gzm-design（果米设计）

- 项目：https://gitee.com/sourcenet/gzm-design ／ https://github.com/LvHuaiSheng/gzm-design
- 许可：MIT
- 版权：Copyright © 2023-present guozimi.cn
- 用途：编辑器服务层的**代码来源**。当前 P0 阶段尚未复制其代码；
  自 P2 起复制或改写其代码时，本节保留其 MIT 许可与版权声明，
  且每个相关文件头部注释标明原始来源。
```

- [ ] **Step 6: Commit**

```bash
git add -A
git commit -m "chore(scaffold): 建 pnpm monorepo 骨架与测试基建

- pnpm workspace + tsconfig.base(strict + noUncheckedIndexedAccess)
- vitest 双 project：core 走 node 环境、renderer-dom 走 jsdom
- @framewright/core 包骨架与冒烟测试
- THIRD-PARTY-NOTICES 登记 LeaferJS 与 gzm-design 的 MIT 与版权行"
```

---

## Task 2: node-schema.ts —— schema 单一真相源

**Files:**
- Create: `packages/core/src/node-schema.ts`
- Test: `packages/core/src/node-schema.test.ts`
- Modify: `packages/core/src/index.ts`

**Interfaces:**
- Consumes: Task 1 的包骨架
- Produces:
  - `SHAPE_TYPES: readonly ['frame','box','img','video']`、`ShapeType`
  - `CanvasNode`、`FrameNode`、`BoxNode`、`ImgNode`、`VideoNode`
  - `isFrameNode(n: CanvasNode): n is FrameNode`（另有 `isBoxNode` / `isImgNode` / `isVideoNode`）
  - `createFrameNode(init)` / `createBoxNode(init)`：`init` 为 `Partial<X> & { fwId: string }`，返回补齐默认值的完整节点

- [ ] **Step 1: 写失败的测试**

`packages/core/src/node-schema.test.ts`:
```ts
import { describe, expect, it } from 'vitest'
import {
  SHAPE_TYPES,
  createBoxNode,
  createFrameNode,
  isBoxNode,
  isFrameNode,
} from './node-schema'

describe('SHAPE_TYPES', () => {
  it('是 frame/box/img/video 四个，且顺序固定', () => {
    expect(SHAPE_TYPES).toEqual(['frame', 'box', 'img', 'video'])
  })
})

describe('createFrameNode', () => {
  it('补齐默认值：位于原点、可见、未锁定、不裁剪、无子节点', () => {
    const frame = createFrameNode({ fwId: 'f1' })
    expect(frame.fwType).toBe('frame')
    expect(frame.x).toBe(0)
    expect(frame.y).toBe(0)
    expect(frame.opacity).toBe(1)
    expect(frame.visible).toBe(true)
    expect(frame.locked).toBe(false)
    expect(frame.clip).toBe(false)
    expect(frame.children).toEqual([])
  })

  it('显式传入的字段覆盖默认值', () => {
    const frame = createFrameNode({ fwId: 'f1', x: 10, width: 800, clip: true })
    expect(frame.x).toBe(10)
    expect(frame.width).toBe(800)
    expect(frame.clip).toBe(true)
  })
})

describe('createBoxNode', () => {
  it('补齐默认值', () => {
    const box = createBoxNode({ fwId: 'b1' })
    expect(box.fwType).toBe('box')
    expect(box.fill).toBe('#CCCCCC')
    expect(box.cornerRadius).toBe(0)
  })
})

describe('类型守卫', () => {
  it('按 fwType 判别', () => {
    const frame = createFrameNode({ fwId: 'f1' })
    const box = createBoxNode({ fwId: 'b1' })
    expect(isFrameNode(frame)).toBe(true)
    expect(isFrameNode(box)).toBe(false)
    expect(isBoxNode(box)).toBe(true)
    expect(isBoxNode(frame)).toBe(false)
  })
})
```

- [ ] **Step 2: 运行测试确认失败**

Run: `pnpm vitest run packages/core/src/node-schema.test.ts`
Expected: FAIL，报 `Failed to resolve import "./node-schema"`

- [ ] **Step 3: 实现 node-schema.ts**

```ts
/** schema 单一真相源。任何 node 字段的增删改只在本文件发生。 */

export const SHAPE_TYPES = ['frame', 'box', 'img', 'video'] as const
export type ShapeType = (typeof SHAPE_TYPES)[number]

export type ObjectFit = 'contain' | 'cover' | 'fill'

/** 所有 node 共有的字段。fw 前缀者为 framewright 语义字段，其余为几何/呈现字段。 */
export interface BaseNode {
  fwId: string
  fwType: ShapeType
  name: string
  /** 相对父节点左上角的偏移，不是画布绝对坐标 */
  x: number
  y: number
  width: number
  height: number
  /** 度 */
  rotation: number
  /** 0–1 */
  opacity: number
  visible: boolean
  locked: boolean
}

export interface FrameNode extends BaseNode {
  fwType: 'frame'
  clip: boolean
  background: string | null
  /** 数组顺序即 z 序，靠后者画在上面。不设 zIndex 字段。 */
  children: CanvasNode[]
}

export interface BoxNode extends BaseNode {
  fwType: 'box'
  fill: string
  cornerRadius: number
}

export interface ImgNode extends BaseNode {
  fwType: 'img'
  src: string
  fit: ObjectFit
}

export interface VideoNode extends BaseNode {
  fwType: 'video'
  src: string
  poster: string | null
  fit: ObjectFit
}

export type CanvasNode = FrameNode | BoxNode | ImgNode | VideoNode

export function isFrameNode(node: CanvasNode): node is FrameNode {
  return node.fwType === 'frame'
}
export function isBoxNode(node: CanvasNode): node is BoxNode {
  return node.fwType === 'box'
}
export function isImgNode(node: CanvasNode): node is ImgNode {
  return node.fwType === 'img'
}
export function isVideoNode(node: CanvasNode): node is VideoNode {
  return node.fwType === 'video'
}

function baseDefaults(fwId: string, fwType: ShapeType): BaseNode {
  return {
    fwId,
    fwType,
    name: '',
    x: 0,
    y: 0,
    width: 100,
    height: 100,
    rotation: 0,
    opacity: 1,
    visible: true,
    locked: false,
  }
}

type Init<T extends CanvasNode> = Partial<Omit<T, 'fwType'>> & { fwId: string }

export function createFrameNode(init: Init<FrameNode>): FrameNode {
  return {
    ...baseDefaults(init.fwId, 'frame'),
    fwType: 'frame',
    clip: false,
    background: null,
    children: [],
    ...init,
  }
}

export function createBoxNode(init: Init<BoxNode>): BoxNode {
  return {
    ...baseDefaults(init.fwId, 'box'),
    fwType: 'box',
    fill: '#CCCCCC',
    cornerRadius: 0,
    ...init,
  }
}

export function createImgNode(init: Init<ImgNode>): ImgNode {
  return {
    ...baseDefaults(init.fwId, 'img'),
    fwType: 'img',
    src: '',
    fit: 'contain',
    ...init,
  }
}

export function createVideoNode(init: Init<VideoNode>): VideoNode {
  return {
    ...baseDefaults(init.fwId, 'video'),
    fwType: 'video',
    src: '',
    poster: null,
    fit: 'contain',
    ...init,
  }
}
```

- [ ] **Step 4: 从 index 导出并运行测试**

`packages/core/src/index.ts` 追加：
```ts
export * from './node-schema'
```

Run: `pnpm vitest run packages/core/src/node-schema.test.ts`
Expected: PASS，6 个用例全绿

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "feat(core): 定义 node schema 单一真相源

- CanvasNode 判别联合：frame/box/img/video，fwId + fwType 带 fw 前缀
- SHAPE_TYPES 全集常量，供两侧渲染器注册表校验使用
- 四个类型守卫与四个带默认值的工厂函数
- 只有 frame 有 children；数组顺序即 z 序，不设 zIndex"
```

---

## Task 3: node-tree.ts —— 树操作与绝对坐标累加

**Files:**
- Create: `packages/core/src/node-tree.ts`
- Test: `packages/core/src/node-tree.test.ts`
- Modify: `packages/core/src/index.ts`

**Interfaces:**
- Consumes: Task 2 的 `CanvasNode` / `FrameNode` / `isFrameNode`
- Produces:
  - `walkTree(root: CanvasNode, visit: (node: CanvasNode, absolute: Point) => void): void` —— 深度优先，`absolute` 为累加后的绝对坐标
  - `findNodeById(root: CanvasNode, fwId: string): CanvasNode | null`
  - `collectNodeIds(root: CanvasNode): string[]`
  - `getAbsolutePosition(root: CanvasNode, fwId: string): Point | null`
  - `Point = { x: number; y: number }`

- [ ] **Step 1: 写失败的测试**

`packages/core/src/node-tree.test.ts`:
```ts
import { describe, expect, it } from 'vitest'
import { createBoxNode, createFrameNode } from './node-schema'
import {
  collectNodeIds,
  findNodeById,
  getAbsolutePosition,
  walkTree,
} from './node-tree'

/** root(0,0) > inner frame(100,50) > box(10,20)  →  box 绝对坐标应为 (110,70) */
function buildTree() {
  const box = createBoxNode({ fwId: 'box', x: 10, y: 20 })
  const inner = createFrameNode({ fwId: 'inner', x: 100, y: 50, children: [box] })
  return createFrameNode({ fwId: 'root', x: 0, y: 0, children: [inner] })
}

describe('getAbsolutePosition', () => {
  it('逐层累加父节点偏移', () => {
    expect(getAbsolutePosition(buildTree(), 'box')).toEqual({ x: 110, y: 70 })
  })

  it('根节点的绝对坐标就是它自己的 x/y', () => {
    expect(getAbsolutePosition(buildTree(), 'root')).toEqual({ x: 0, y: 0 })
  })

  it('找不到返回 null', () => {
    expect(getAbsolutePosition(buildTree(), 'nope')).toBeNull()
  })
})

describe('collectNodeIds', () => {
  it('按深度优先、children 数组顺序返回全部 fwId', () => {
    expect(collectNodeIds(buildTree())).toEqual(['root', 'inner', 'box'])
  })

  it('同层按数组顺序，即 z 序从下到上', () => {
    const a = createBoxNode({ fwId: 'a' })
    const b = createBoxNode({ fwId: 'b' })
    const root = createFrameNode({ fwId: 'root', children: [a, b] })
    expect(collectNodeIds(root)).toEqual(['root', 'a', 'b'])
  })
})

describe('findNodeById', () => {
  it('能找到深层节点', () => {
    expect(findNodeById(buildTree(), 'box')?.fwId).toBe('box')
  })

  it('找不到返回 null', () => {
    expect(findNodeById(buildTree(), 'nope')).toBeNull()
  })
})

describe('walkTree', () => {
  it('每个节点访问一次，并带上绝对坐标', () => {
    const seen: Array<[string, number, number]> = []
    walkTree(buildTree(), (node, absolute) => {
      seen.push([node.fwId, absolute.x, absolute.y])
    })
    expect(seen).toEqual([
      ['root', 0, 0],
      ['inner', 100, 50],
      ['box', 110, 70],
    ])
  })
})
```

- [ ] **Step 2: 运行测试确认失败**

Run: `pnpm vitest run packages/core/src/node-tree.test.ts`
Expected: FAIL，报 `Failed to resolve import "./node-tree"`

- [ ] **Step 3: 实现 node-tree.ts**

```ts
import { type CanvasNode, isFrameNode } from './node-schema'

export interface Point {
  x: number
  y: number
}

/**
 * 深度优先遍历。`absolute` 是逐层累加父节点 x/y 后的画布绝对坐标——
 * node 自身的 x/y 是相对父节点的，见 docs/domain.md §3.3 规则 1。
 */
export function walkTree(
  root: CanvasNode,
  visit: (node: CanvasNode, absolute: Point) => void,
): void {
  const step = (node: CanvasNode, parentX: number, parentY: number): void => {
    const x = parentX + node.x
    const y = parentY + node.y
    visit(node, { x, y })
    if (isFrameNode(node)) {
      for (const child of node.children) step(child, x, y)
    }
  }
  step(root, 0, 0)
}

export function findNodeById(root: CanvasNode, fwId: string): CanvasNode | null {
  let found: CanvasNode | null = null
  walkTree(root, (node) => {
    if (found === null && node.fwId === fwId) found = node
  })
  return found
}

export function collectNodeIds(root: CanvasNode): string[] {
  const ids: string[] = []
  walkTree(root, (node) => ids.push(node.fwId))
  return ids
}

export function getAbsolutePosition(root: CanvasNode, fwId: string): Point | null {
  let result: Point | null = null
  walkTree(root, (node, absolute) => {
    if (result === null && node.fwId === fwId) result = absolute
  })
  return result
}
```

- [ ] **Step 4: 从 index 导出并运行测试**

`packages/core/src/index.ts` 追加：
```ts
export * from './node-tree'
```

Run: `pnpm vitest run packages/core/src/node-tree.test.ts`
Expected: PASS，8 个用例全绿

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "feat(core): 树遍历与绝对坐标累加

- walkTree 深度优先，回调带累加后的绝对坐标
- findNodeById / collectNodeIds / getAbsolutePosition 基于 walkTree 复用
- 坐标是相对父节点的，累加由 core 统一负责，渲染器不各自实现"
```

---

## Task 4: renderer-adapter.ts —— 接口定义与注册表完整性校验

**Files:**
- Create: `packages/core/src/renderer-adapter.ts` `packages/core/src/demo-document.ts`
- Test: `packages/core/src/renderer-adapter.test.ts`
- Modify: `packages/core/src/index.ts`

**Interfaces:**
- Consumes: Task 2 的 `SHAPE_TYPES` / `ShapeType` / `FrameNode`；Task 3 的无
- Produces:
  - `RendererId = 'dom' | 'leafer'`
  - `Viewport = { scale: number; offsetX: number; offsetY: number }`、`DEFAULT_VIEWPORT`
  - `Rect = { x: number; y: number; width: number; height: number }`
  - `RenderContext = { root: FrameNode; selection: readonly string[]; viewport: Viewport }`
  - `RendererAdapter`：`id` / `displayName` / `mount(container, ctx)` / `update(ctx)` / `destroy()` / `getRenderedBounds(): Map<string, Rect>`
  - `assertShapeCoverage(rendererId: RendererId, registry: Record<string, unknown>): void`
  - `createDemoDocument(): FrameNode`

- [ ] **Step 1: 写失败的测试**

`packages/core/src/renderer-adapter.test.ts`:
```ts
import { describe, expect, it } from 'vitest'
import { SHAPE_TYPES } from './node-schema'
import { DEFAULT_VIEWPORT, assertShapeCoverage } from './renderer-adapter'
import { createDemoDocument } from './demo-document'
import { collectNodeIds } from './node-tree'

describe('DEFAULT_VIEWPORT', () => {
  it('缩放 1、无偏移', () => {
    expect(DEFAULT_VIEWPORT).toEqual({ scale: 1, offsetX: 0, offsetY: 0 })
  })
})

describe('assertShapeCoverage', () => {
  it('覆盖全部 SHAPE_TYPES 时通过', () => {
    const full = Object.fromEntries(SHAPE_TYPES.map((t) => [t, () => null]))
    expect(() => assertShapeCoverage('dom', full)).not.toThrow()
  })

  it('缺任何一个 fwType 就抛错，并指名渲染器与缺失项', () => {
    const partial = { frame: () => null, box: () => null }
    expect(() => assertShapeCoverage('leafer', partial)).toThrowError(
      /renderer "leafer".*img.*video/s,
    )
  })
})

describe('createDemoDocument', () => {
  it('产出根 frame，含嵌套 frame 与多个 box，且覆盖全部 fwType', () => {
    const doc = createDemoDocument()
    expect(doc.fwType).toBe('frame')
    const ids = collectNodeIds(doc)
    expect(ids.length).toBeGreaterThanOrEqual(6)
    expect(ids[0]).toBe('root')
  })

  it('每次调用返回全新对象，互不共享引用', () => {
    const a = createDemoDocument()
    const b = createDemoDocument()
    expect(a).not.toBe(b)
    expect(a.children[0]).not.toBe(b.children[0])
  })
})
```

- [ ] **Step 2: 运行测试确认失败**

Run: `pnpm vitest run packages/core/src/renderer-adapter.test.ts`
Expected: FAIL，报 `Failed to resolve import "./renderer-adapter"`

- [ ] **Step 3: 实现 renderer-adapter.ts**

```ts
import { SHAPE_TYPES, type FrameNode } from './node-schema'

export type RendererId = 'dom' | 'leafer'

/** 视口属于会话状态：不持久化，但切换渲染器时必须保留。 */
export interface Viewport {
  scale: number
  offsetX: number
  offsetY: number
}

export const DEFAULT_VIEWPORT: Viewport = { scale: 1, offsetX: 0, offsetY: 0 }

export interface Rect {
  x: number
  y: number
  width: number
  height: number
}

/**
 * 渲染所需的全部输入。渲染器是这份数据的无状态投影，
 * 销毁重建不丢东西——这正是运行时切换成立的前提。
 * P0 不含交互回调（无交互），P2 交互补齐时扩展。
 */
export interface RenderContext {
  root: FrameNode
  selection: readonly string[]
  viewport: Viewport
}

export interface RendererAdapter {
  readonly id: RendererId
  readonly displayName: string
  mount(container: HTMLElement, ctx: RenderContext): void
  update(ctx: RenderContext): void
  /** 必须彻底清干净：DOM、监听器、RAF、渲染器实例 */
  destroy(): void
  /**
   * 自证「每个节点被画在了哪」，key 为 fwId，值为画布坐标系下的矩形。
   * 这是对照实验的量具：Leafer 画在 canvas 上没有 DOM 元素可量，
   * 只能由渲染器自报。见 docs/architecture.md §8.1。
   */
  getRenderedBounds(): Map<string, Rect>
}

/**
 * 注册表完整性校验：把「两版同步开发」从纪律变成机器可检查的约束。
 * 类型系统守编译期，本函数守运行时与动态注册。见 docs/architecture.md §10.3。
 */
export function assertShapeCoverage(
  rendererId: RendererId,
  registry: Record<string, unknown>,
): void {
  const missing = SHAPE_TYPES.filter((type) => registry[type] === undefined)
  if (missing.length > 0) {
    throw new Error(
      `Shape registry incomplete for renderer "${rendererId}": missing ${missing.join(', ')}. ` +
        `Every renderer must cover all SHAPE_TYPES (${SHAPE_TYPES.join(', ')}). ` +
        `If a shape is genuinely unsupported, register an explicit unsupported implementation instead of omitting it.`,
    )
  }
}
```

- [ ] **Step 4: 实现 demo-document.ts**

```ts
import {
  createBoxNode,
  createFrameNode,
  createImgNode,
  createVideoNode,
  type FrameNode,
} from './node-schema'

/**
 * P0 的固定 demo 文档。两个渲染器与全部对照测试共用同一份输入——
 * 输入不同，对照就没有意义。
 *
 * 刻意覆盖：嵌套 frame（验证坐标累加）、同层多 box（验证 z 序 = 数组顺序）、
 * img 与 video（验证注册表里的 unsupported 占位实现真的被走到）。
 */
export function createDemoDocument(): FrameNode {
  const boxBack = createBoxNode({
    fwId: 'box-back',
    name: '底层方块',
    x: 40,
    y: 40,
    width: 200,
    height: 140,
    fill: '#4C8BF5',
  })

  const boxFront = createBoxNode({
    fwId: 'box-front',
    name: '上层方块',
    x: 120,
    y: 100,
    width: 200,
    height: 140,
    fill: '#F55A4C',
    cornerRadius: 16,
  })

  const nestedBox = createBoxNode({
    fwId: 'nested-box',
    name: '嵌套方块',
    x: 20,
    y: 20,
    width: 120,
    height: 80,
    fill: '#3DBE7B',
  })

  const innerFrame = createFrameNode({
    fwId: 'inner-frame',
    name: '内层画框',
    x: 380,
    y: 60,
    width: 240,
    height: 180,
    clip: true,
    background: '#F2F2F2',
    children: [nestedBox],
  })

  const img = createImgNode({
    fwId: 'img-1',
    name: '图片占位',
    x: 40,
    y: 300,
    width: 160,
    height: 100,
  })

  const video = createVideoNode({
    fwId: 'video-1',
    name: '视频占位',
    x: 240,
    y: 300,
    width: 160,
    height: 100,
  })

  return createFrameNode({
    fwId: 'root',
    name: '画布',
    x: 0,
    y: 0,
    width: 800,
    height: 450,
    background: '#FFFFFF',
    children: [boxBack, boxFront, innerFrame, img, video],
  })
}
```

- [ ] **Step 5: 从 index 导出并运行测试**

`packages/core/src/index.ts` 追加：
```ts
export * from './renderer-adapter'
export * from './demo-document'
```

Run: `pnpm vitest run packages/core/src/renderer-adapter.test.ts`
Expected: PASS，5 个用例全绿

- [ ] **Step 6: 全量测试与类型检查**

```bash
pnpm test
pnpm typecheck
```
Expected: 全部通过，`typecheck` 无输出

- [ ] **Step 7: Commit**

```bash
git add -A
git commit -m "feat(core): RendererAdapter 接口、注册表完整性校验与 demo 文档

- RenderContext = 树 + 选中集 + 视口，渲染器是它的无状态投影
- getRenderedBounds 作为正式接口：Leafer 画在 canvas 上无 DOM 可量,
  只能由渲染器自报，它是对照实验的量具
- assertShapeCoverage 把「两版同步」变成装载时报错的硬约束
- createDemoDocument 为两侧与全部对照测试提供同一份输入"
```

---

## Task 5: renderer-dom —— DOM/React 渲染器

**Files:**
- Create: `packages/renderer-dom/package.json` `packages/renderer-dom/tsconfig.json`
- Create: `packages/renderer-dom/src/node-style.ts` `packages/renderer-dom/src/shapes/registry.tsx` `packages/renderer-dom/src/index.tsx`
- Test: `packages/renderer-dom/src/node-style.test.ts` `packages/renderer-dom/src/index.test.tsx`

**Interfaces:**
- Consumes: `@framewright/core` 的 `CanvasNode` / `RenderContext` / `RendererAdapter` / `Rect` / `assertShapeCoverage` / `walkTree` / `SHAPE_TYPES`
- Produces: `createDomRenderer(): RendererAdapter`（`id === 'dom'`，`displayName === 'HTML / DOM'`）；`toNodeStyle(node, absolute)`

- [ ] **Step 1: 写 node-style 的失败测试**

`packages/renderer-dom/src/node-style.test.ts`:
```ts
import { describe, expect, it } from 'vitest'
import { createBoxNode } from '@framewright/core'
import { toNodeStyle } from './node-style'

describe('toNodeStyle', () => {
  it('用绝对坐标定位，几何字段逐个映射', () => {
    const box = createBoxNode({ fwId: 'b', x: 10, y: 20, width: 30, height: 40 })
    const style = toNodeStyle(box, { x: 110, y: 220 })
    expect(style.position).toBe('absolute')
    expect(style.left).toBe('110px')
    expect(style.top).toBe('220px')
    expect(style.width).toBe('30px')
    expect(style.height).toBe('40px')
  })

  it('visible=false 渲染为 display:none，而不是从树里摘掉', () => {
    const box = createBoxNode({ fwId: 'b', visible: false })
    expect(toNodeStyle(box, { x: 0, y: 0 }).display).toBe('none')
  })

  it('rotation 与 opacity 映射到 transform 与 opacity', () => {
    const box = createBoxNode({ fwId: 'b', rotation: 45, opacity: 0.5 })
    const style = toNodeStyle(box, { x: 0, y: 0 })
    expect(style.transform).toBe('rotate(45deg)')
    expect(style.opacity).toBe(0.5)
  })

  it('不把 node 的字段整体带进样式对象', () => {
    const box = createBoxNode({ fwId: 'b', name: 'x' })
    const style = toNodeStyle(box, { x: 0, y: 0 }) as Record<string, unknown>
    expect(style['fwId']).toBeUndefined()
    expect(style['name']).toBeUndefined()
  })
})
```

- [ ] **Step 2: 创建包并运行测试确认失败**

`packages/renderer-dom/package.json`:
```json
{
  "name": "@framewright/renderer-dom",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.tsx",
  "types": "./src/index.tsx",
  "exports": { ".": "./src/index.tsx" },
  "dependencies": {
    "@framewright/core": "workspace:*"
  },
  "peerDependencies": {
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "react": "19.2.8",
    "react-dom": "19.2.8",
    "@types/react": "^19.2.0",
    "@types/react-dom": "^19.2.0"
  }
}
```

`packages/renderer-dom/tsconfig.json`:
```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": { "outDir": "dist", "rootDir": "src" },
  "include": ["src"],
  "references": [{ "path": "../core" }]
}
```

```bash
pnpm install
pnpm vitest run packages/renderer-dom/src/node-style.test.ts
```
Expected: FAIL，报 `Failed to resolve import "./node-style"`

- [ ] **Step 3: 实现 node-style.ts**

```ts
import type { CanvasNode, Point } from '@framewright/core'
import type { CSSProperties } from 'react'

/**
 * node → CSS 的**逐字段显式映射**。
 * 🔴 绝不允许 `{...node}` 展开：node 的字段名与渲染器属性大量同名，
 * 展开会让新增字段静默泄漏、改名不报错。见 docs/domain.md §3.3 规则 7。
 */
export function toNodeStyle(node: CanvasNode, absolute: Point): CSSProperties {
  return {
    position: 'absolute',
    left: `${absolute.x}px`,
    top: `${absolute.y}px`,
    width: `${node.width}px`,
    height: `${node.height}px`,
    opacity: node.opacity,
    transform: `rotate(${node.rotation}deg)`,
    transformOrigin: 'top left',
    display: node.visible ? 'block' : 'none',
    boxSizing: 'border-box',
  }
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `pnpm vitest run packages/renderer-dom/src/node-style.test.ts`
Expected: PASS，4 个用例全绿

- [ ] **Step 5: 实现 shapes/registry.tsx**

```tsx
import {
  SHAPE_TYPES,
  assertShapeCoverage,
  isBoxNode,
  isFrameNode,
  type CanvasNode,
  type Point,
  type ShapeType,
} from '@framewright/core'
import type { CSSProperties, ReactNode } from 'react'
import { toNodeStyle } from '../node-style'

export interface ShapeProps {
  node: CanvasNode
  absolute: Point
  selected: boolean
  children?: ReactNode
}

export type ShapeComponent = (props: ShapeProps) => ReactNode

const SELECTED_OUTLINE = '2px solid #5B8091'

function withSelection(style: CSSProperties, selected: boolean): CSSProperties {
  return selected ? { ...style, outline: SELECTED_OUTLINE } : style
}

function FrameShape({ node, absolute, selected, children }: ShapeProps): ReactNode {
  const base = toNodeStyle(node, absolute)
  const style: CSSProperties = {
    ...base,
    background: isFrameNode(node) && node.background !== null ? node.background : 'transparent',
    overflow: isFrameNode(node) && node.clip ? 'hidden' : 'visible',
  }
  return (
    <div data-fw-id={node.fwId} data-fw-type="frame" style={withSelection(style, selected)}>
      {children}
    </div>
  )
}

function BoxShape({ node, absolute, selected }: ShapeProps): ReactNode {
  const base = toNodeStyle(node, absolute)
  const style: CSSProperties = {
    ...base,
    background: isBoxNode(node) ? node.fill : 'transparent',
    borderRadius: isBoxNode(node) ? `${node.cornerRadius}px` : undefined,
  }
  return <div data-fw-id={node.fwId} data-fw-type="box" style={withSelection(style, selected)} />
}

/**
 * P0 尚未实现的 shape 的显式占位。
 * 不留空——留空会让 assertShapeCoverage 报错，而「暂不支持」是一种被记录的状态，
 * 正好喂给 docs/architecture.md §8.2 的实现成本对照表。
 */
function makeUnsupportedShape(type: ShapeType): ShapeComponent {
  return function UnsupportedShape({ node, absolute, selected }: ShapeProps): ReactNode {
    const style: CSSProperties = {
      ...toNodeStyle(node, absolute),
      background: 'repeating-linear-gradient(45deg,#EEE,#EEE 8px,#DDD 8px,#DDD 16px)',
      border: '1px dashed #999',
    }
    return (
      <div
        data-fw-id={node.fwId}
        data-fw-type={type}
        data-fw-unsupported="true"
        style={withSelection(style, selected)}
      />
    )
  }
}

export const DOM_SHAPES: Record<ShapeType, ShapeComponent> = {
  frame: FrameShape,
  box: BoxShape,
  img: makeUnsupportedShape('img'),
  video: makeUnsupportedShape('video'),
}

assertShapeCoverage('dom', DOM_SHAPES)

export { SHAPE_TYPES }
```

- [ ] **Step 6: 写渲染器的失败测试**

`packages/renderer-dom/src/index.test.tsx`:
```tsx
import { afterEach, describe, expect, it } from 'vitest'
import { DEFAULT_VIEWPORT, createDemoDocument, type RenderContext } from '@framewright/core'
import { createDomRenderer } from './index'

function makeContext(selection: readonly string[] = []): RenderContext {
  return { root: createDemoDocument(), selection, viewport: DEFAULT_VIEWPORT }
}

let container: HTMLElement | null = null

afterEach(() => {
  container?.remove()
  container = null
})

function mountRenderer(ctx: RenderContext) {
  container = document.createElement('div')
  document.body.appendChild(container)
  const renderer = createDomRenderer()
  renderer.mount(container, ctx)
  return renderer
}

describe('createDomRenderer', () => {
  it('id 与 displayName 固定', () => {
    const renderer = createDomRenderer()
    expect(renderer.id).toBe('dom')
    expect(renderer.displayName).toBe('HTML / DOM')
  })

  it('mount 后每个节点都有对应 DOM 元素', async () => {
    const renderer = mountRenderer(makeContext())
    await Promise.resolve()
    expect(container!.querySelector('[data-fw-id="box-back"]')).not.toBeNull()
    expect(container!.querySelector('[data-fw-id="nested-box"]')).not.toBeNull()
    renderer.destroy()
  })

  it('img/video 渲染为显式的 unsupported 占位', async () => {
    const renderer = mountRenderer(makeContext())
    await Promise.resolve()
    expect(
      container!.querySelector('[data-fw-id="img-1"][data-fw-unsupported="true"]'),
    ).not.toBeNull()
    renderer.destroy()
  })

  it('getRenderedBounds 报告绝对坐标，嵌套节点已累加父偏移', async () => {
    const renderer = mountRenderer(makeContext())
    await Promise.resolve()
    const bounds = renderer.getRenderedBounds()
    // inner-frame(380,60) > nested-box(20,20) → (400,80)
    expect(bounds.get('nested-box')).toEqual({ x: 400, y: 80, width: 120, height: 80 })
    renderer.destroy()
  })

  it('destroy 后容器清空', async () => {
    const renderer = mountRenderer(makeContext())
    await Promise.resolve()
    renderer.destroy()
    await Promise.resolve()
    expect(container!.innerHTML).toBe('')
    expect(renderer.getRenderedBounds().size).toBe(0)
  })

  it('update 换上选中态后，被选中节点带 outline', async () => {
    const renderer = mountRenderer(makeContext())
    await Promise.resolve()
    renderer.update(makeContext(['box-front']))
    await Promise.resolve()
    const el = container!.querySelector('[data-fw-id="box-front"]') as HTMLElement
    expect(el.style.outline).toContain('#5B8091')
    renderer.destroy()
  })
})
```

- [ ] **Step 7: 运行测试确认失败**

Run: `pnpm vitest run packages/renderer-dom/src/index.test.tsx`
Expected: FAIL，报 `createDomRenderer is not exported`

- [ ] **Step 8: 实现 index.tsx**

```tsx
import {
  isFrameNode,
  assertShapeCoverage,
  type CanvasNode,
  type Point,
  type Rect,
  type RenderContext,
  type RendererAdapter,
} from '@framewright/core'
import type { ReactNode } from 'react'
import { createRoot, type Root } from 'react-dom/client'
import { DOM_SHAPES } from './shapes/registry'

function renderNode(
  node: CanvasNode,
  parentAbsolute: Point,
  selection: readonly string[],
  bounds: Map<string, Rect>,
): ReactNode {
  const absolute: Point = { x: parentAbsolute.x + node.x, y: parentAbsolute.y + node.y }
  bounds.set(node.fwId, {
    x: absolute.x,
    y: absolute.y,
    width: node.width,
    height: node.height,
  })

  const Shape = DOM_SHAPES[node.fwType]
  const children = isFrameNode(node)
    ? node.children.map((child) => renderNode(child, absolute, selection, bounds))
    : undefined

  return (
    <Shape
      key={node.fwId}
      node={node}
      absolute={absolute}
      selected={selection.includes(node.fwId)}
    >
      {children}
    </Shape>
  )
}

export function createDomRenderer(): RendererAdapter {
  assertShapeCoverage('dom', DOM_SHAPES)

  let root: Root | null = null
  let bounds = new Map<string, Rect>()

  const draw = (ctx: RenderContext): void => {
    if (root === null) return
    bounds = new Map<string, Rect>()
    const { scale, offsetX, offsetY } = ctx.viewport
    root.render(
      <div
        style={{
          position: 'relative',
          width: '100%',
          height: '100%',
          overflow: 'hidden',
          transform: `translate(${offsetX}px, ${offsetY}px) scale(${scale})`,
          transformOrigin: 'top left',
        }}
      >
        {renderNode(ctx.root, { x: 0, y: 0 }, ctx.selection, bounds)}
      </div>,
    )
  }

  return {
    id: 'dom',
    displayName: 'HTML / DOM',

    mount(container, ctx) {
      root = createRoot(container)
      draw(ctx)
    },

    update(ctx) {
      draw(ctx)
    },

    destroy() {
      root?.unmount()
      root = null
      bounds = new Map<string, Rect>()
    },

    getRenderedBounds() {
      return new Map(bounds)
    },
  }
}
```

- [ ] **Step 9: 运行测试确认通过**

Run: `pnpm vitest run packages/renderer-dom`
Expected: PASS，10 个用例全绿（node-style 4 + index 6）

- [ ] **Step 10: Commit**

```bash
git add -A
git commit -m "feat(renderer-dom): DOM/React 渲染器最小实现

- toNodeStyle 逐字段映射 node → CSS，禁止 {...node} 展开
- shape 注册表覆盖全部 SHAPE_TYPES，img/video 注册显式 unsupported 占位
  而非留空——「暂不支持」是被记录的状态,喂给实现成本对照表
- getRenderedBounds 在渲染时顺路收集，报告累加后的绝对坐标
- destroy 走 React unmount 并清空 bounds"
```

---

## Task 6: renderer-leafer —— LeaferJS 渲染器

**Files:**
- Create: `packages/renderer-leafer/package.json` `packages/renderer-leafer/tsconfig.json`
- Create: `packages/renderer-leafer/src/node-props.ts` `packages/renderer-leafer/src/shapes/registry.ts` `packages/renderer-leafer/src/index.ts`
- Test: `packages/renderer-leafer/src/node-props.test.ts`（纯函数，Vitest node 环境）
- Modify: `vitest.config.ts`（新增 `renderer-leafer` project）

**Interfaces:**
- Consumes: `@framewright/core` 同 Task 5；`leafer-ui` 的 `Leafer` / `Box` / `Rect`
- Produces: `createLeaferRenderer(): RendererAdapter`（`id === 'leafer'`，`displayName === 'LeaferJS'`）；`toLeaferProps(node, absolute)`

> **注意**：LeaferJS 需要真实 canvas 上下文，jsdom 提供不了。本任务的 Vitest 只覆盖**纯映射函数**；真实渲染与 `getRenderedBounds` 的验证在 Task 8 的 Playwright 里做。

- [ ] **Step 1: 创建包**

`packages/renderer-leafer/package.json`:
```json
{
  "name": "@framewright/renderer-leafer",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": { ".": "./src/index.ts" },
  "dependencies": {
    "@framewright/core": "workspace:*",
    "leafer-ui": "2.2.9"
  }
}
```

`packages/renderer-leafer/tsconfig.json`:
```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": { "outDir": "dist", "rootDir": "src" },
  "include": ["src"],
  "references": [{ "path": "../core" }]
}
```

`vitest.config.ts` 的 `projects` 数组追加：
```ts
{
  test: {
    name: 'renderer-leafer',
    environment: 'node',
    include: ['packages/renderer-leafer/src/**/*.test.ts'],
  },
},
```

```bash
pnpm install
```

- [ ] **Step 2: 写 node-props 的失败测试**

`packages/renderer-leafer/src/node-props.test.ts`:
```ts
import { describe, expect, it } from 'vitest'
import { createBoxNode, createFrameNode } from '@framewright/core'
import { toLeaferProps } from './node-props'

describe('toLeaferProps', () => {
  it('用绝对坐标，几何字段逐个映射', () => {
    const box = createBoxNode({ fwId: 'b', x: 10, y: 20, width: 30, height: 40 })
    const props = toLeaferProps(box, { x: 110, y: 220 })
    expect(props.x).toBe(110)
    expect(props.y).toBe(220)
    expect(props.width).toBe(30)
    expect(props.height).toBe(40)
  })

  it('rotation 与 opacity 与 visible 直接映射', () => {
    const box = createBoxNode({ fwId: 'b', rotation: 45, opacity: 0.5, visible: false })
    const props = toLeaferProps(box, { x: 0, y: 0 })
    expect(props.rotation).toBe(45)
    expect(props.opacity).toBe(0.5)
    expect(props.visible).toBe(false)
  })

  it('🔴 不把 node 的 framewright 字段泄漏进 Leafer 属性', () => {
    const box = createBoxNode({ fwId: 'b', name: 'x' })
    const props = toLeaferProps(box, { x: 0, y: 0 }) as Record<string, unknown>
    expect(props['fwId']).toBeUndefined()
    expect(props['fwType']).toBeUndefined()
    expect(props['locked']).toBeUndefined()
    expect(props['children']).toBeUndefined()
  })

  it('frame 与 box 走同一份基础映射，形状差异由 registry 负责', () => {
    const frame = createFrameNode({ fwId: 'f', x: 5, y: 5, width: 50, height: 50 })
    const props = toLeaferProps(frame, { x: 5, y: 5 })
    expect(props.x).toBe(5)
    expect(props.width).toBe(50)
  })
})
```

- [ ] **Step 3: 运行测试确认失败**

Run: `pnpm vitest run packages/renderer-leafer/src/node-props.test.ts`
Expected: FAIL，报 `Failed to resolve import "./node-props"`

- [ ] **Step 4: 实现 node-props.ts**

```ts
import type { CanvasNode, Point } from '@framewright/core'

/** Leafer 节点的基础几何属性。刻意只列我们真正映射的字段。 */
export interface LeaferBaseProps {
  x: number
  y: number
  width: number
  height: number
  rotation: number
  opacity: number
  visible: boolean
}

/**
 * node → Leafer 属性的**逐字段显式映射**。
 * 🔴 绝不允许 `new Rect({...node})`：Leafer 的 x/y/width/height/rotation/opacity/
 * visible/name/children 与我方字段全部同名，展开会静默覆盖并让新增字段泄漏。
 * 见 docs/domain.md §3.3 规则 7。
 */
export function toLeaferProps(node: CanvasNode, absolute: Point): LeaferBaseProps {
  return {
    x: absolute.x,
    y: absolute.y,
    width: node.width,
    height: node.height,
    rotation: node.rotation,
    opacity: node.opacity,
    visible: node.visible,
  }
}
```

- [ ] **Step 5: 运行测试确认通过**

Run: `pnpm vitest run packages/renderer-leafer/src/node-props.test.ts`
Expected: PASS，4 个用例全绿

- [ ] **Step 6: 实现 shapes/registry.ts**

```ts
import {
  assertShapeCoverage,
  isBoxNode,
  isFrameNode,
  type CanvasNode,
  type Point,
  type ShapeType,
} from '@framewright/core'
import { Box, Rect, type IUI } from 'leafer-ui'
import { toLeaferProps } from '../node-props'

export interface ShapeContext {
  node: CanvasNode
  absolute: Point
  selected: boolean
}

/** 返回一个 Leafer 节点；frame 返回容器（可 add 子节点），其余返回叶子。 */
export type ShapeFactory = (ctx: ShapeContext) => IUI

const SELECTED_STROKE = '#5B8091'

function applySelection(ui: IUI, selected: boolean): IUI {
  if (selected) {
    ui.stroke = SELECTED_STROKE
    ui.strokeWidth = 2
  }
  return ui
}

const createFrame: ShapeFactory = ({ node, absolute, selected }) => {
  const box = new Box({
    ...toLeaferProps(node, absolute),
    fill: isFrameNode(node) && node.background !== null ? node.background : undefined,
    overflow: isFrameNode(node) && node.clip ? 'hide' : 'show',
  })
  return applySelection(box, selected)
}

const createBox: ShapeFactory = ({ node, absolute, selected }) => {
  const rect = new Rect({
    ...toLeaferProps(node, absolute),
    fill: isBoxNode(node) ? node.fill : undefined,
    cornerRadius: isBoxNode(node) ? node.cornerRadius : 0,
  })
  return applySelection(rect, selected)
}

/**
 * P0 尚未实现的 shape 的显式占位——与 renderer-dom 的 unsupported 占位对应。
 * 留空会让 assertShapeCoverage 报错，这是刻意的。
 */
function makeUnsupportedShape(): ShapeFactory {
  return ({ node, absolute, selected }) => {
    const rect = new Rect({
      ...toLeaferProps(node, absolute),
      fill: '#DDDDDD',
      stroke: '#999999',
      strokeWidth: 1,
      dashPattern: [4, 4],
    })
    return applySelection(rect, selected)
  }
}

export const LEAFER_SHAPES: Record<ShapeType, ShapeFactory> = {
  frame: createFrame,
  box: createBox,
  img: makeUnsupportedShape(),
  video: makeUnsupportedShape(),
}

assertShapeCoverage('leafer', LEAFER_SHAPES)
```

> 注：这里的 `...toLeaferProps(...)` 展开的是**映射函数的返回值**（一个只含 7 个几何字段的窄对象），不是 node 本身——这正是铁律要求的形态。

- [ ] **Step 7: 实现 index.ts**

```ts
import {
  assertShapeCoverage,
  isFrameNode,
  type CanvasNode,
  type Point,
  type Rect as CoreRect,
  type RenderContext,
  type RendererAdapter,
} from '@framewright/core'
import { Leafer, type IUI } from 'leafer-ui'
import { LEAFER_SHAPES } from './shapes/registry'

export function createLeaferRenderer(): RendererAdapter {
  assertShapeCoverage('leafer', LEAFER_SHAPES)

  let leafer: Leafer | null = null
  let bounds = new Map<string, CoreRect>()

  const buildNode = (
    node: CanvasNode,
    parentAbsolute: Point,
    selection: readonly string[],
    parent: IUI | Leafer,
  ): void => {
    const absolute: Point = { x: parentAbsolute.x + node.x, y: parentAbsolute.y + node.y }
    bounds.set(node.fwId, {
      x: absolute.x,
      y: absolute.y,
      width: node.width,
      height: node.height,
    })

    const factory = LEAFER_SHAPES[node.fwType]
    const ui = factory({ node, absolute, selected: selection.includes(node.fwId) })
    parent.add(ui)

    if (isFrameNode(node)) {
      // 子节点同样使用画布绝对坐标，故父级传 leafer 根而非 ui，避免双重偏移
      for (const child of node.children) {
        buildNode(child, absolute, selection, leafer as Leafer)
      }
    }
  }

  const draw = (ctx: RenderContext): void => {
    if (leafer === null) return
    leafer.clear()
    bounds = new Map<string, CoreRect>()
    leafer.scale = ctx.viewport.scale
    leafer.x = ctx.viewport.offsetX
    leafer.y = ctx.viewport.offsetY
    buildNode(ctx.root, { x: 0, y: 0 }, ctx.selection, leafer)
  }

  return {
    id: 'leafer',
    displayName: 'LeaferJS',

    mount(container, ctx) {
      leafer = new Leafer({ view: container })
      draw(ctx)
    },

    update(ctx) {
      draw(ctx)
    },

    destroy() {
      leafer?.destroy()
      leafer = null
      bounds = new Map<string, CoreRect>()
    },

    getRenderedBounds() {
      return new Map(bounds)
    },
  }
}
```

- [ ] **Step 8: 类型检查与全量测试**

```bash
pnpm typecheck
pnpm test
```
Expected: `typecheck` 无输出；`test` 全绿（core 19 + renderer-dom 10 + renderer-leafer 4）

- [ ] **Step 9: Commit**

```bash
git add -A
git commit -m "feat(renderer-leafer): LeaferJS 渲染器最小实现

- toLeaferProps 逐字段映射，窄返回对象只含 7 个几何字段
  —— 防止 Leafer 同名属性(x/y/width/height/rotation/opacity/visible)被 node 静默覆盖
- shape 注册表覆盖全集，img/video 注册虚线占位与 DOM 侧对应
- Leafer 依赖真实 canvas，本包单测只覆盖纯映射函数,
  真实渲染与 getRenderedBounds 的验证放 Playwright(Task 8)"
```

---

## Task 7: apps/web —— RendererHost 与切换开关

**Files:**
- Create: `apps/web/package.json` `apps/web/tsconfig.json` `apps/web/next.config.ts` `apps/web/next-env.d.ts`
- Create: `apps/web/app/layout.tsx` `apps/web/app/page.tsx` `apps/web/components/renderer-host.tsx`

**Interfaces:**
- Consumes: `createDomRenderer()` / `createLeaferRenderer()` / `createDemoDocument()` / `DEFAULT_VIEWPORT`
- Produces: 可运行的 Next.js 应用；页面上带 `data-testid="renderer-switch"` 的切换按钮与 `data-testid="canvas-container"` 的画布容器，供 Task 8 的 Playwright 使用

- [ ] **Step 1: 创建应用包**

`apps/web/package.json`:
```json
{
  "name": "@framewright/web",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "next dev -p 3100",
    "build": "next build",
    "start": "next start -p 3100"
  },
  "dependencies": {
    "@framewright/core": "workspace:*",
    "@framewright/renderer-dom": "workspace:*",
    "@framewright/renderer-leafer": "workspace:*",
    "next": "15.5.22",
    "react": "19.2.8",
    "react-dom": "19.2.8"
  },
  "devDependencies": {
    "@types/react": "^19.2.0",
    "@types/react-dom": "^19.2.0"
  }
}
```

`apps/web/tsconfig.json`:
```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "composite": false,
    "noEmit": true,
    "plugins": [{ "name": "next" }],
    "paths": { "@/*": ["./*"] }
  },
  "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts"],
  "exclude": ["node_modules"]
}
```

`apps/web/next.config.ts`:
```ts
import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  // workspace 包以 TS 源码形式发布，交给 Next 转译
  transpilePackages: [
    '@framewright/core',
    '@framewright/renderer-dom',
    '@framewright/renderer-leafer',
  ],
}

export default nextConfig
```

`apps/web/next-env.d.ts`:
```ts
/// <reference types="next" />
/// <reference types="next/image-types/global" />
```

- [ ] **Step 2: 实现 RendererHost**

`apps/web/components/renderer-host.tsx`:
```tsx
'use client'

import {
  DEFAULT_VIEWPORT,
  createDemoDocument,
  type RenderContext,
  type RendererAdapter,
} from '@framewright/core'
import { createDomRenderer } from '@framewright/renderer-dom'
import { createLeaferRenderer } from '@framewright/renderer-leafer'
import { useEffect, useMemo, useRef, useState } from 'react'

type Factory = () => RendererAdapter

/** 注册在这里的每一项都必须实现同一个 RendererAdapter，上层不做品牌分支判断。 */
const RENDERERS: ReadonlyArray<{ id: string; label: string; create: Factory }> = [
  { id: 'dom', label: 'HTML / DOM', create: createDomRenderer },
  { id: 'leafer', label: 'LeaferJS', create: createLeaferRenderer },
]

export function RendererHost() {
  const containerRef = useRef<HTMLDivElement>(null)
  const adapterRef = useRef<RendererAdapter | null>(null)
  const [activeIndex, setActiveIndex] = useState(0)

  // 会话状态住在这里，不在渲染器内部——切换时原样传给新渲染器
  const [selection, setSelection] = useState<readonly string[]>(['box-front'])
  const [viewport] = useState(DEFAULT_VIEWPORT)
  const root = useMemo(() => createDemoDocument(), [])

  const ctx: RenderContext = { root, selection, viewport }
  const ctxRef = useRef(ctx)
  ctxRef.current = ctx

  useEffect(() => {
    const container = containerRef.current
    if (container === null) return
    const entry = RENDERERS[activeIndex]
    if (entry === undefined) return

    const adapter = entry.create()
    adapterRef.current = adapter
    adapter.mount(container, ctxRef.current)

    return () => {
      adapter.destroy()
      adapterRef.current = null
    }
  }, [activeIndex])

  useEffect(() => {
    adapterRef.current?.update(ctx)
  }, [ctx])

  const active = RENDERERS[activeIndex]

  return (
    <main style={{ fontFamily: 'system-ui, sans-serif', padding: 16 }}>
      <div style={{ display: 'flex', gap: 12, alignItems: 'center', marginBottom: 12 }}>
        <button
          type="button"
          data-testid="renderer-switch"
          onClick={() => setActiveIndex((i) => (i + 1) % RENDERERS.length)}
          style={{ padding: '6px 14px', cursor: 'pointer' }}
        >
          切换渲染器
        </button>
        <span data-testid="active-renderer">{active?.label ?? ''}</span>
        <button
          type="button"
          data-testid="select-box-back"
          onClick={() => setSelection(['box-back'])}
          style={{ padding: '6px 14px', cursor: 'pointer' }}
        >
          选中底层方块
        </button>
        <span data-testid="selection">{selection.join(',')}</span>
      </div>

      <div
        ref={containerRef}
        data-testid="canvas-container"
        style={{ width: 800, height: 450, border: '1px solid #DDD', position: 'relative' }}
      />
    </main>
  )
}
```

- [ ] **Step 3: 实现页面**

`apps/web/app/layout.tsx`:
```tsx
import type { ReactNode } from 'react'

export const metadata = { title: 'framewright P0' }

export default function RootLayout({ children }: { children: ReactNode }) {
  return (
    <html lang="zh-CN">
      <body style={{ margin: 0 }}>{children}</body>
    </html>
  )
}
```

`apps/web/app/page.tsx`:
```tsx
'use client'

import dynamic from 'next/dynamic'

// LeaferJS 是纯客户端库，需要真实 canvas；关闭 SSR 避免服务端求值
const RendererHost = dynamic(
  () => import('@/components/renderer-host').then((m) => m.RendererHost),
  { ssr: false },
)

export default function Page() {
  return <RendererHost />
}
```

- [ ] **Step 4: 安装并启动，人工确认两版都能画出来**

```bash
pnpm install
pnpm dev
```

打开 http://localhost:3100 ，确认：
1. 默认显示 `HTML / DOM`，画布上有蓝色方块、红色圆角方块、灰底内层画框含绿色方块、两个虚线/斜纹占位；
2. 点「切换渲染器」后标签变 `LeaferJS`，画面内容与位置**看起来一致**；
3. 再点一次切回 DOM。

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "feat(web): RendererHost 与运行时切换开关

- 会话状态(选中集/视口)与 node 树住在 host,经 RenderContext 单向流入渲染器
- 切换 = 旧 adapter.destroy() → 新 adapter.mount(同一 container, 同一 ctx)
- RENDERERS 注册表驱动,上层无渲染器品牌分支判断
- 画布页 dynamic ssr:false —— LeaferJS 是纯客户端库"
```

---

## Task 8: Playwright —— Leafer 真实渲染、几何对照与切换测试

**Files:**
- Create: `playwright.config.ts` `e2e/leafer-render.spec.ts` `e2e/parity.spec.ts` `e2e/switch.spec.ts`
- Modify: `package.json`（加 `@playwright/test` 依赖）
- Modify: `apps/web/components/renderer-host.tsx`（暴露测试用的 bounds 读取入口）

**Interfaces:**
- Consumes: Task 7 的 `data-testid`；两个渲染器的 `getRenderedBounds()`
- Produces: `window.__fwGetBounds(): Record<string, Rect>`（仅供 e2e 读取当前渲染器的 bounds）

- [ ] **Step 1: 安装 Playwright**

```bash
pnpm add -Dw @playwright/test@^1.62.1
pnpm exec playwright install chromium
```

- [ ] **Step 2: 写配置**

`playwright.config.ts`:
```ts
import { defineConfig } from '@playwright/test'

export default defineConfig({
  testDir: './e2e',
  use: { baseURL: 'http://localhost:3100' },
  webServer: {
    command: 'pnpm --filter @framewright/web dev',
    url: 'http://localhost:3100',
    reuseExistingServer: true,
    timeout: 120_000,
  },
})
```

- [ ] **Step 3: 在 host 暴露 bounds 读取入口**

`apps/web/components/renderer-host.tsx` 的 `useEffect`（装载那个）中，`adapter.mount(...)` 之后追加：

```tsx
    ;(window as unknown as Record<string, unknown>)['__fwGetBounds'] = () =>
      Object.fromEntries(adapter.getRenderedBounds())
```

同一 `useEffect` 的清理函数中，`adapter.destroy()` 之后追加：

```tsx
      delete (window as unknown as Record<string, unknown>)['__fwGetBounds']
```

- [ ] **Step 4: 写 Leafer 真实渲染测试**

`e2e/leafer-render.spec.ts`:
```ts
import { expect, test } from '@playwright/test'

test('Leafer 渲染器能在真实浏览器里挂载并报告全部节点的 bounds', async ({ page }) => {
  await page.goto('/')
  await page.getByTestId('renderer-switch').click()
  await expect(page.getByTestId('active-renderer')).toHaveText('LeaferJS')

  const bounds = await page.evaluate(() =>
    (window as unknown as { __fwGetBounds: () => Record<string, unknown> }).__fwGetBounds(),
  )

  expect(Object.keys(bounds).sort()).toEqual(
    ['box-back', 'box-front', 'img-1', 'inner-frame', 'nested-box', 'root', 'video-1'].sort(),
  )
  expect(bounds['nested-box']).toEqual({ x: 400, y: 80, width: 120, height: 80 })
})

test('Leafer 挂载后容器里出现 canvas 元素', async ({ page }) => {
  await page.goto('/')
  await page.getByTestId('renderer-switch').click()
  await expect(page.getByTestId('canvas-container').locator('canvas')).toHaveCount(1)
})
```

- [ ] **Step 5: 写几何对照测试**

`e2e/parity.spec.ts`:
```ts
import { expect, test } from '@playwright/test'

type Bounds = Record<string, { x: number; y: number; width: number; height: number }>

async function readBounds(page: import('@playwright/test').Page): Promise<Bounds> {
  return page.evaluate(
    () => (window as unknown as { __fwGetBounds: () => Bounds }).__fwGetBounds(),
  ) as Promise<Bounds>
}

test('两个渲染器对同一份 node 树报告完全相同的几何', async ({ page }) => {
  await page.goto('/')
  await expect(page.getByTestId('active-renderer')).toHaveText('HTML / DOM')
  const domBounds = await readBounds(page)

  await page.getByTestId('renderer-switch').click()
  await expect(page.getByTestId('active-renderer')).toHaveText('LeaferJS')
  const leaferBounds = await readBounds(page)

  expect(Object.keys(leaferBounds).sort()).toEqual(Object.keys(domBounds).sort())
  for (const fwId of Object.keys(domBounds)) {
    expect(leaferBounds[fwId], `节点 ${fwId} 的几何在两个渲染器间不一致`).toEqual(
      domBounds[fwId],
    )
  }
})
```

- [ ] **Step 6: 写切换测试**

`e2e/switch.spec.ts`:
```ts
import { expect, test } from '@playwright/test'

test('切换渲染器后选中态保留', async ({ page }) => {
  await page.goto('/')
  await page.getByTestId('select-box-back').click()
  await expect(page.getByTestId('selection')).toHaveText('box-back')

  await page.getByTestId('renderer-switch').click()
  await expect(page.getByTestId('active-renderer')).toHaveText('LeaferJS')

  // 选中态住在 host 不在渲染器，切换不该丢
  await expect(page.getByTestId('selection')).toHaveText('box-back')
})

test('切换后旧渲染器的 DOM 被清干净，容器里只剩新渲染器的产物', async ({ page }) => {
  await page.goto('/')
  const container = page.getByTestId('canvas-container')

  // DOM 渲染器：有带 data-fw-id 的元素，无 canvas
  await expect(container.locator('[data-fw-id]').first()).toBeVisible()
  await expect(container.locator('canvas')).toHaveCount(0)

  await page.getByTestId('renderer-switch').click()
  await expect(page.getByTestId('active-renderer')).toHaveText('LeaferJS')

  // Leafer 渲染器：有 canvas，DOM 渲染器留下的元素必须一个不剩
  await expect(container.locator('canvas')).toHaveCount(1)
  await expect(container.locator('[data-fw-id]')).toHaveCount(0)
})

test('切回 DOM 渲染器后 canvas 被清干净', async ({ page }) => {
  await page.goto('/')
  const container = page.getByTestId('canvas-container')

  await page.getByTestId('renderer-switch').click()
  await expect(container.locator('canvas')).toHaveCount(1)

  await page.getByTestId('renderer-switch').click()
  await expect(page.getByTestId('active-renderer')).toHaveText('HTML / DOM')
  await expect(container.locator('canvas')).toHaveCount(0)
  await expect(container.locator('[data-fw-id]').first()).toBeVisible()
})
```

- [ ] **Step 7: 写几何基线回归测试**

`e2e/geometry-baseline.spec.ts`:
```ts
import { expect, test } from '@playwright/test'

interface Rect {
  x: number
  y: number
  width: number
  height: number
}
type Bounds = Record<string, Rect>

const CASES = [
  { label: 'HTML / DOM', switches: 0, snapshot: 'bounds-dom.json' },
  { label: 'LeaferJS', switches: 1, snapshot: 'bounds-leafer.json' },
] as const

for (const testCase of CASES) {
  test(`几何基线：${testCase.label}`, async ({ page }) => {
    await page.goto('/')
    for (let i = 0; i < testCase.switches; i += 1) {
      await page.getByTestId('renderer-switch').click()
    }
    await expect(page.getByTestId('active-renderer')).toHaveText(testCase.label)

    const bounds = (await page.evaluate(
      () => (window as unknown as { __fwGetBounds: () => Bounds }).__fwGetBounds(),
    )) as Bounds

    // key 排序后再序列化，否则遍历顺序变化会造成假 diff
    const stable = Object.fromEntries(
      Object.keys(bounds)
        .sort()
        .map((fwId) => [fwId, bounds[fwId]]),
    )
    expect(JSON.stringify(stable, null, 2)).toMatchSnapshot(testCase.snapshot)
  })
}
```

**这是回归防线的第一道**：基线一旦落盘，此后任何让节点位置或尺寸改变的改动都会让它变红。

- 首次生成基线：`pnpm e2e --update-snapshots`
- 之后基线文件**必须提交进 git**，否则回归防线不存在
- 将来要改基线，只能显式跑 `--update-snapshots` 并**在 commit message 里写清为什么几何变了**——「几何变了」永远是一个被人看见的决定，不是静默漂移

- [ ] **Step 8: 生成基线并运行全部 e2e**

```bash
pnpm e2e --update-snapshots
pnpm e2e
```
Expected: 第一条生成 `e2e/geometry-baseline.spec.ts-snapshots/` 下两个基线文件；第二条 8 passed

> **如果 parity 测试失败**，那正是 P0 的价值所在——它说明两个渲染器对同一份数据的理解不一致。**先查是不是坐标累加口径不同**（Leafer 的 `Box` 子节点默认相对父容器定位，而我们统一用画布绝对坐标，见 `renderer-leafer/src/index.ts` 里把子节点 `add` 到 leafer 根而非父 `ui` 的处理）。修好后把差异记进 `docs/architecture.md` §8.2 实现成本对照表——**这类差异就是选型结论的原始素材**。

- [ ] **Step 9: 跑提交门禁**

```bash
pnpm verify
```
Expected: `typecheck` 无输出、`test` 全绿、`e2e` 8 passed。**三条任一失败都不许进入下一步。**

- [ ] **Step 10: Commit（含基线文件）**

```bash
git add -A
git commit -m "test(e2e): Leafer 真实渲染、几何对照、切换与几何基线回归

- leafer-render：Leafer 需真实 canvas,jsdom 跑不了,故走 Playwright
- parity：两个渲染器对同一份 node 树必须报告完全相同的几何
- switch：切换后选中态保留,且旧渲染器的 DOM/canvas 一个不剩
  —— 这两条守着「状态不许渗进渲染器」这条铁律
- geometry-baseline：两侧 bounds 快照落盘并提交,此后几何变化必须显式
  --update-snapshots 才能过,把静默漂移变成看得见的决定
- 根脚本 verify = typecheck && test && e2e,作为提交门禁"
```

---

## Task 9: 收口 —— 文档回填与进度更新

**Files:**
- Modify: `docs/architecture.md`（§8.2 填入 P0 的实现成本记录）
- Modify: `docs/progress.md`
- Modify: `docs/lessons.md`（若 P0 过程中产生了可复用结论）

**Interfaces:**
- Consumes: Task 1–8 的实际耗时与踩坑记录
- Produces: 可交给 P1 的状态快照

- [ ] **Step 1: 回填实现成本对照表**

在 `docs/architecture.md` §8.2 的表格中，为 P0 实际做的每个 shape 各补一行，格式：

```markdown
| frame 容器渲染 | Leafer `Box` + `overflow:'hide'`，Xh | DOM `<div>` + `overflow:hidden`，Xh | 备注 |
| box 矩形渲染   | Leafer `Rect` + cornerRadius，Xh     | DOM `<div>` + borderRadius，Xh      | 备注 |
| 坐标系对齐     | 说明实际踩到的差异与耗时              | 同左                                 | 见 Task 8 Step 7 |
```

**耗时如实记，含踩坑返工的时间。** 这张表失真了整个项目就白做。

- [ ] **Step 2: 更新项目进度**

把 `docs/progress.md` 的 P0 一行标为完成，填入完成日期、实际 commit 数、遗留问题。

- [ ] **Step 3: 若有可复用结论，追加进 lessons**

判据：这条结论换个项目还成立吗？成立才写。一次性过程噪音不写。

- [ ] **Step 4: Commit**

```bash
git add -A
git commit -m "docs(p0): 回填实现成本对照表与项目进度

- architecture §8.2 补 P0 各 shape 的两侧实现成本(含返工耗时)
- progress.md 标记 P0 完成
- lessons 追加 P0 产生的可复用结论"
```

---

## P0 完成判据

全部满足才算完成，缺一不可：

1. `pnpm verify` 一条命令全绿（= `typecheck && test && e2e`）
2. 单元测试覆盖：`core` 的**每一个公开导出**都有测试；两侧的映射函数**各有一条「不泄漏」测试**（断言 `fwId` / `fwType` / `locked` / `children` 没进渲染器属性）
3. `pnpm e2e` 8 个用例全绿（leafer-render 2 + parity 1 + switch 3 + geometry-baseline 2）
4. **几何基线文件已生成并提交进 git**——没提交等于没有回归防线
5. `pnpm dev` 后浏览器里能一键切换两版画布，**画面一致**
6. `docs/architecture.md` §8.2 有 P0 的实现成本记录，耗时如实（含返工）
7. `docs/progress.md` 已更新

**P0 明确不做**（做了就是超范围）：拖拽、框选、缩放平移的 UI、后端、持久化、真实 AI 生成、img/video 的真实渲染、gzm-design 代码搬运。
