# Kimi 工作指令 —— framewright P0 收口（T9b + T12）

仓库根目录 `E:\code\github\framewright`。

## 0. 先读

> ⚠️ 你的工具只自动加载 `AGENTS.md`，下面 2–4 项必须自己用读文件工具打开。

1. `AGENTS.md` —— 行为规则与禁止项，优先级最高（自动加载）
2. `docs/progress.md` —— 当前进度
3. `docs/renderer-contract.md` —— **T11 已定案的渲染器契约**，你 T10 的作答是它的一半依据
4. `docs/plans/2026-08-03-P0-编排与分工.md` —— 交接协议与回报格式

## 0.1 先说一件与你有关的事

你在 T10 作答里称「demo 文档的子节点恰好全在 root 直下，父相对坐标 == 绝对坐标，故 parity 测不出坐标系分歧，P0 是假绿」。

**这一条经核对 `packages/core/src/demo-document.ts` 原文后不成立**：`nested-box` 挂在偏移 `(380,60)` 的 `inner-frame` 下，父相对 `(20,20)` ≠ 绝对 `(400,80)`。契约文档已撤回该论据。

说这件事不是追责——**你 T10 里关于 Leafer 技术处境的每一条都经受住了核对**（内建手势、`scaleFixed`、`hitRadius`、事件列表无 CANCEL 等），那部分质量很高。区别在于：

- **你那一侧的技术处境** → 你是唯一在场者，可信
- **关于共享代码的事实断言** → 请先打开文件核对再写

下面两个任务里，凡是涉及共享代码现状的判断，**请引用你实际读到的文件与行号**。

## 1. 你的两个任务

### T9b — 报告 `renderer-leafer` 的实现成本（**不写代码，只填表**）

写入 `docs/plans/answers/T9-cost-leafer.md`。

这是 `docs/architecture.md` §8.2「实现成本对照表」的原始素材，**是本项目的核心交付物之一**——对真实项目而言，「装个插件 2 小时 vs 手写 2 天」比「帧率 58 还是 60」更能决定选哪个方案。

对你在 P0 实现 `renderer-leafer` 时做的**每一项**，各写一行：

| 功能项 | 你的做法（具体到 API / 手段） | 实际耗时 | 踩坑与返工 | 有没有让步 |
|---|---|---|---|---|

至少覆盖：

- `mount` / `update` / `destroy` 三个生命周期
- frame 容器渲染（含 `clip` 裁剪、`background`）
- box 渲染（含 `fill`、`cornerRadius`）
- img / video 的 unsupported 占位
- 父子坐标累加
- z 序
- 选中态描边
- viewport（scale / offset）的应用
- `getRenderedBounds()` 的收集
- shape 注册表与完整性校验

**硬性要求**：

1. **耗时如实写，含踩坑返工。** 不确定就写区间并标注「估算」。
2. **「有没有让步」这一栏最值钱。** 你在 T10 已经点出一个典型：**场景图被打平，`frame` 的 `clip` 实际空转**——那就是一次让步：用 Leafer 方案本该能做到的容器裁剪，实际没有用容器实现。这类如实写全。
3. T6 / T10 报过的「接口摩擦」，凡与实现成本相关的**归并进来**。

commit：`docs(t9): renderer-leafer 侧 P0 实现成本报告`

### T12 — 「场景图打平 vs 嵌套」评估（**不写代码，只出评估**）

写入 `docs/plans/answers/T12-flat-vs-nested.md`。

**背景**：P0 的 Leafer 实现把所有节点以画布绝对坐标 `add` 到 leafer 根，没有使用 `frame` 的容器性。你在 T10-E-Q4 指出这笔税会在交互上兑现（`clip` 空转、拖 frame 要自维护「fwId → 全部后代」映射），并建议 P2 前重评。

**现在评估，但不要改代码。** 需要回答：

1. **现状核实**：打开 `packages/renderer-leafer/src/index.ts` 与 `shapes/registry.ts`，**引用实际行号**说明当前是怎么组织场景图的。
2. **改成嵌套要动什么**：具体到函数与行数量级。`getRenderedBounds()` 的绝对坐标语义会不会受影响（契约要求它恒为画布绝对坐标）？
3. **嵌套能白拿什么**：`clip` 真正生效？拖拽 frame 不用维护后代映射？子树整体显隐？还有别的吗？
4. **嵌套会引入什么新代价**：Leafer 的嵌套坐标系与我们「node 的 x/y 是父相对」的 schema 对不对得上？`getRenderedBounds` 要不要额外换算？多层嵌套下的性能？
5. **对 P2 每一项交互的影响**：按 `docs/renderer-contract.md` §4 的手势状态机与 §5 的 core 函数清单，逐项说哪些会变简单、哪些会变复杂。
6. **你的推荐**：打平还是嵌套，以及**判据是什么**。若推荐改，给出改动的建议时机（P0 内 / P1 / P2 开工前）与风险。

**这份评估的价值在于诚实**：如果打平其实够用、改嵌套是过度工程，就直说；反过来也一样。**不要为了显得彻底而推荐重构。**

commit：`docs(t12): 场景图打平 vs 嵌套评估`

## 2. 硬性纪律

- **只做 T9b 和 T12**，两个都不写实现代码
- **不许改计划、不许扩范围**
- **不许 `git push`**，只 commit
- 涉及共享代码现状的判断，**引用实际读到的文件与行号**

## 3. 回报格式

```
任务号：T9b / T12
状态：完成 / 受阻
新建文件：<路径列表>
commit：<sha> <message 首行>
偏差：<与指令不一致处；没有就写「无」>
```
