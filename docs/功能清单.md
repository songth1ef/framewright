# 功能清单 —— 已实现 / 未实现

> 2026-08-04 夜盘点。**按「打通程度」分三态，不是两态**——很多功能两端都写了但没接上，
> 那既不算「已实现」也不算「没做」，而且这正是体感上「好像没做」的原因。

| 图例 | 含义 |
|---|---|
| ✅ | **端到端打通**，在跑起来的应用里真的能用 |
| 🟡 | **两端都写了、各自有测试，但没接上** —— 跑起来用不到 |
| ⬜ | 没做 |

---

## 前端 · 画布交互

| 功能 | 状态 | 备注 |
|---|---|---|
| 无限画布平移（滚轮 / 中键 / 空格+左键） | ✅ | 两个渲染器都有 |
| 缩放（`Ctrl`+滚轮，以光标为锚点，10%–400%） | ✅ | 两侧 |
| 点选 / `Shift` 增选 / 框选（相交即选中） | ✅ | 两侧 |
| 拖拽移动（多选整体移动） | ✅ | 两侧 |
| 四角等比缩放（`MIN_NODE_SIZE=32`） | ✅ | 两侧。多选只画包围框不给控制点（刻意） |
| 键盘（`Ctrl+A` / `Esc` / `Delete` / 方向键微调） | ✅ | 两侧。焦点在输入框时不劫持 |
| 六态光标 | ✅ | 两侧 |
| 选中 / hover overlay（`1/scale` 补偿不随缩放变粗） | ✅ | 两侧 |
| **渲染器一键切换** | ✅ | 切换后视口与选中保留 |
| 工具栏 UI（缩放按钮 / 适应画布 / 100%） | ⬜ | X1、X2 |
| 空画布引导 / 快捷键帮助面板 | ⬜ | X3、X4 |

## 前端 · 节点与内容

| 功能 | 状态 | 备注 |
|---|---|---|
| `frame` / `box` 渲染 | ✅ | 两侧 |
| 生成单元四态（空 / 生成中骨架屏 / 成功 / 失败） | ✅ | 两侧，共享样式常量 |
| 溯源连线（贝塞尔，高亮，`1/scale` 补偿） | ✅ | 两侧共用 `computeConnectionCurve` |
| `img` / `video` 纯素材节点 | 🟡 | schema 有、注册表有 unsupported 占位，**未做真实渲染** |
| **视频节点内嵌可播放播放器** | ⬜ | **C3，开环 #9，可能直接决定选型** |
| 生成参数面板（prompt 输入 / 模型 / 尺寸） | ⬜ | G5 |
| 节点 hover 业务工具条（重生成 / 下载 / 裁剪…） | ⬜ | 只有 `onNodeAction` 通道，没有 UI |

## 前端 · 撤销

| 功能 | 状态 | 备注 |
|---|---|---|
| 操作栈（`record` / `undo` / `redo`，redo 段截断） | ✅ | `core/history.ts` |
| `Ctrl+Z` / `Ctrl+Shift+Z` | ✅ | |
| 删除后撤销，**节点与连线一起恢复** | ✅ | `inboundRefs` 对称持有 |
| 一次多选手势整体撤销（`batch`） | ✅ | |
| **每个操作写透后端** | ✅ | `POST /api/documents/[id]/history` |
| **跨会话撤销**（刷新后仍能撤销上一次操作） | 🟡 | 代码通了，**但依赖下面的自动保存，实际用不成** |

---

## 后端

| 功能 | 状态 | 备注 |
|---|---|---|
| Prisma + SQLite 接入、迁移 | ✅ | |
| `Document` / `HistoryEntry` 表 | ✅ | |
| `Project` / `Session` / `Message` / `Asset` / `Generation` 表 | 🟡 | **schema 与迁移已就位，没有任何存取代码与接口** |
| `server-core` 的 Document 存取（纯 TS 零 Next 依赖） | ✅ | |
| `server-core` 的历史存取（追加 / 读取 / 200 条裁剪） | ✅ | |
| `GET/PUT /api/documents/[id]` | ✅ | |
| `GET/POST /api/documents/[id]/history` | ✅ | |
| `GET/POST /api/documents`（列表 / 新建） | ✅ | |
| Project / Session / Message / Asset 的存取与接口 | ⬜ | B3–B6 |
| **对话回溯双向索引**（产物 ↔ 消息） | ⬜ | B5，你要的「sessionId 回溯到那条对话」 |
| 素材存储（`AssetStorage` 接口 + 本地实现） | ⬜ | B6 |
| provider 接口 + mock（异步提交→轮询→占位素材） | ⬜ | G1 |
| LLM 接口（对话 + 意图转生成参数） | ⬜ | 只有设计，见 `backend-domain.md` §6 |

---

## 🔴 打通程度 —— 这一节解释了「感觉没做」

| 链路 | 状态 | 卡在哪 |
|---|---|---|
| 首页列出画布 / 新建 | ✅ | `document-list.tsx` 真的在调 `/api/documents` |
| `/canvas/[documentId]` 路由 + 加载态 + 404 | ✅ | 页面都在 |
| 打开画布时从后端加载操作日志 | ✅ | `loadServerHistory()` |
| **每次操作写后端** | ✅ | `record()` → `POST .../history` |
| **🔴 画布内容自动保存** | ⬜ | **F6 没做** |

### F6 没做的具体后果

`server-history.ts` 第 12 行的注释写得很明白：

> *「平时的 `historySeq` 持久化由 Document **防抖保存（F6）**携带 `getHistorySeq()` 完成」*

**也就是说**：`document.root`（画布内容本身）**只在「撤销之后又产生新操作」这一条特定路径下才被 `PUT` 一次**，平时的拖动、缩放、删除**都不写回 document**。

**实际表现**：

- 操作日志在后端 ✅
- 但 `document.root` 停留在旧状态
- **刷新页面 → 画布内容回到旧的** ❌
- 连带「跨会话撤销」也用不成——日志对得上，内容对不上

**这就是你体感「自动保存和撤销都没做」的真实原因**：撤销的后端写入做了，**但没有自动保存兜底，整条链在实际使用中是断的**。

---

## 优先级建议

| 优先 | 事项 | 理由 |
|---|---|---|
| **P0** | **F6 防抖自动保存** | 一件事打通整条持久化链。没有它，前面所有后端工作在应用里都体现不出来 |
| **P1** | **C3 视频播放器实测** + **R2 canvas 文本输入实测** | 开环 #9 与 Excalidraw 六年未解的问题，**直接决定选型结论** |
| P2 | G1 provider mock → G4 生成四态真实流转 | 让画布「活」起来 |
| P3 | B3–B6（Project / Session / Asset / **回溯双向索引**） | 你要的对话回溯 |
| P4 | X1–X4 工具栏与引导 UI | 体验打磨 |
