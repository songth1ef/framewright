node_modules/
dist/
.next/
*.tsbuildinfo
test-results/
playwright-report/
.env
.env.local
*.db
*.db-journal

# agent 之间的异步信箱：过程通信，不是项目资产。协议见 docs/agents-mail.md
docs/agents-mail/

# 演示视频产物,不入库
demo-video/

# 实测探针:源码入库(可复现),但构建产物与素材不入库
# 素材是 2.4MB 二进制,git 无法增量压缩,入库等于永久负重;
# 探针结果 JSON 例外 —— 那是证据,体积也小,要留。
# 注意 `**/` 前缀不能省:含斜杠的 pattern 会被锚定到 .gitignore 所在目录,
# 写成 `probes/.dist/` 只能匹配仓库根下的,匹配不到 packages/*/probes/。
**/probes/.dist/
**/probes/fixtures/*.webm
**/probes/fixtures/*.mp4
